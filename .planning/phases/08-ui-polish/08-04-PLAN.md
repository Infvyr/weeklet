---
phase: 08-ui-polish
plan: "04"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - lib/presentation/screens/expenses/widgets/list/expense_item_view.dart
  - lib/presentation/screens/income/widgets/list/income_item_view.dart
  - lib/presentation/screens/categories/widgets/category_item_view.dart
  - lib/presentation/screens/settings/widgets/currency_selection_sheet.dart
  - lib/presentation/screens/settings/widgets/theme_selection_sheet.dart
  - lib/presentation/screens/settings/widgets/language_selection_sheet.dart
autonomous: true
requirements:
  - UX-05

must_haves:
  truths:
    - "Edit expense sheet (_editExpense) is wrapped with ScrollConfiguration(behavior: const ScrollBehavior())"
    - "Edit income sheet (_editIncome) is wrapped with ScrollConfiguration(behavior: const ScrollBehavior())"
    - "Edit category sheet (_editCategory) is wrapped with ScrollConfiguration(behavior: const ScrollBehavior())"
    - "CurrencySelectionSheet.show builder is wrapped with ScrollConfiguration(behavior: const ScrollBehavior())"
    - "ThemeSelectionSheet.show builder is wrapped with ScrollConfiguration(behavior: const ScrollBehavior())"
    - "LanguageSelectionSheet.show builder is wrapped with ScrollConfiguration(behavior: const ScrollBehavior())"
  artifacts:
    - path: "lib/presentation/screens/expenses/widgets/list/expense_item_view.dart"
      provides: "Modal sheet scrollbar opt-out"
      contains: "ScrollConfiguration"
    - path: "lib/presentation/screens/income/widgets/list/income_item_view.dart"
      provides: "Modal sheet scrollbar opt-out"
      contains: "ScrollConfiguration"
    - path: "lib/presentation/screens/categories/widgets/category_item_view.dart"
      provides: "Modal sheet scrollbar opt-out"
      contains: "ScrollConfiguration"
    - path: "lib/presentation/screens/settings/widgets/currency_selection_sheet.dart"
      provides: "Modal sheet scrollbar opt-out"
      contains: "ScrollConfiguration"
    - path: "lib/presentation/screens/settings/widgets/theme_selection_sheet.dart"
      provides: "Modal sheet scrollbar opt-out"
      contains: "ScrollConfiguration"
    - path: "lib/presentation/screens/settings/widgets/language_selection_sheet.dart"
      provides: "Modal sheet scrollbar opt-out"
      contains: "ScrollConfiguration"
  key_links:
    - from: "all 6 showModalBottomSheet builders"
      to: "ScrollBehavior (Flutter SDK default)"
      via: "ScrollConfiguration(behavior: const ScrollBehavior())"
      pattern: "ScrollConfiguration"
---

<objective>
Wrap the remaining 6 modal bottom sheet callers with ScrollConfiguration(behavior: const ScrollBehavior()) to prevent the WeekletScrollBehavior global scrollbar from appearing inside modal sheets (D-11).

Purpose: Completes UX-05 scrollbar opt-out for all 8 modal sheets (Plans 02 and 03 handled the other 2).
Output: All 6 remaining sheet builders opt out of custom scroll behavior.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/08-ui-polish/08-RESEARCH.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/08-ui-polish/08-PATTERNS.md

<interfaces>
<!-- Two patterns for the wrap, depending on builder return structure: -->

<!-- Pattern A — files returning SizedBox (expense_item_view, income_item_view, category_item_view) -->
// CURRENT:
builder: (_) => SizedBox(
  height: context.screenHeight * 0.8,
  child: EditExpenseFormView(expense),  // or EditIncomeFormView/EditCategoryFormView
),

// AFTER:
builder: (_) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: SizedBox(
    height: context.screenHeight * 0.8,
    child: EditExpenseFormView(expense),
  ),
),

<!-- Pattern B — settings sheets returning widget directly (no SizedBox wrapper) -->
// CURRENT:
builder: (context) => const CurrencySelectionSheet(),   // or ThemeSelectionSheet, LanguageSelectionSheet

// AFTER:
builder: (context) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: const CurrencySelectionSheet(),
),

<!-- No new imports needed in any of these files — ScrollConfiguration is in package:flutter/material.dart -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modal sheet opt-out — item view widgets (expense, income, category) (D-11)</name>
  <files>
    lib/presentation/screens/expenses/widgets/list/expense_item_view.dart,
    lib/presentation/screens/income/widgets/list/income_item_view.dart,
    lib/presentation/screens/categories/widgets/category_item_view.dart
  </files>
  <read_first>
    - lib/presentation/screens/expenses/widgets/list/expense_item_view.dart (read _editExpense method — find the builder: line)
    - lib/presentation/screens/income/widgets/list/income_item_view.dart (read _editIncome method — find the builder: line)
    - lib/presentation/screens/categories/widgets/category_item_view.dart (read _editCategory method — find the builder: line)
  </read_first>
  <action>
Apply the same ScrollConfiguration wrap to all three files. Each uses Pattern A (SizedBox wrapper).

--- expense_item_view.dart (_editExpense method) ---

Find the builder in _editExpense. Current builder:
```dart
builder: (_) => SizedBox(
  height: context.screenHeight * 0.8,
  child: EditExpenseFormView(expense),
),
```

Replace with:
```dart
builder: (_) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: SizedBox(
    height: context.screenHeight * 0.8,
    child: EditExpenseFormView(expense),
  ),
),
```

--- income_item_view.dart (_editIncome method) ---

Find the builder in _editIncome. Current builder:
```dart
builder: (_) => SizedBox(
  height: context.screenHeight * 0.8,
  child: EditIncomeFormView(income),
),
```

Replace with:
```dart
builder: (_) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: SizedBox(
    height: context.screenHeight * 0.8,
    child: EditIncomeFormView(income),
  ),
),
```

--- category_item_view.dart (_editCategory method) ---

Find the builder in _editCategory. Current builder:
```dart
builder: (_) => SizedBox(
  height: context.screenHeight * 0.8,
  child: EditCategoryFormView(category),
),
```

Replace with:
```dart
builder: (_) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: SizedBox(
    height: context.screenHeight * 0.8,
    child: EditCategoryFormView(category),
  ),
),
```

No new imports needed in any of these three files — ScrollConfiguration is in package:flutter/material.dart, already imported in each file.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter analyze lib/presentation/screens/expenses/widgets/list/expense_item_view.dart lib/presentation/screens/income/widgets/list/income_item_view.dart lib/presentation/screens/categories/widgets/category_item_view.dart 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - expense_item_view.dart contains: ScrollConfiguration(
    - expense_item_view.dart contains: behavior: const ScrollBehavior()
    - income_item_view.dart contains: ScrollConfiguration(
    - income_item_view.dart contains: behavior: const ScrollBehavior()
    - category_item_view.dart contains: ScrollConfiguration(
    - category_item_view.dart contains: behavior: const ScrollBehavior()
    - fvm flutter analyze on all three files exits 0 with no errors
  </acceptance_criteria>
  <done>Edit expense, income, and category sheets opt out of WeekletScrollBehavior scrollbar.</done>
</task>

<task type="auto">
  <name>Task 2: Modal sheet opt-out — settings selection sheets (currency, theme, language) (D-11)</name>
  <files>
    lib/presentation/screens/settings/widgets/currency_selection_sheet.dart,
    lib/presentation/screens/settings/widgets/theme_selection_sheet.dart,
    lib/presentation/screens/settings/widgets/language_selection_sheet.dart
  </files>
  <read_first>
    - lib/presentation/screens/settings/widgets/currency_selection_sheet.dart (read the static show method — builder returns widget directly, no SizedBox)
    - lib/presentation/screens/settings/widgets/theme_selection_sheet.dart (read the static show method)
    - lib/presentation/screens/settings/widgets/language_selection_sheet.dart (read the static show method)
  </read_first>
  <action>
Apply Pattern B (direct widget return, no SizedBox) to all three settings sheets.

--- currency_selection_sheet.dart (CurrencySelectionSheet.show static method) ---

Current static show method (lines ~12-15):
```dart
static Future<void> show(BuildContext context) => showModalBottomSheet<void>(
  context: context,
  builder: (context) => const CurrencySelectionSheet(),
);
```

Replace builder parameter:
```dart
static Future<void> show(BuildContext context) => showModalBottomSheet<void>(
  context: context,
  builder: (context) => ScrollConfiguration(
    behavior: const ScrollBehavior(),
    child: const CurrencySelectionSheet(),
  ),
);
```

--- theme_selection_sheet.dart (ThemeSelectionSheet.show static method) ---

Current builder: builder: (context) => const ThemeSelectionSheet()

Replace builder parameter:
```dart
builder: (context) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: const ThemeSelectionSheet(),
),
```

--- language_selection_sheet.dart (LanguageSelectionSheet.show static method) ---

Current builder: builder: (context) => const LanguageSelectionSheet()

Replace builder parameter:
```dart
builder: (context) => ScrollConfiguration(
  behavior: const ScrollBehavior(),
  child: const LanguageSelectionSheet(),
),
```

No new imports needed — ScrollConfiguration is in package:flutter/material.dart, already imported in each file.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter analyze lib/presentation/screens/settings/widgets/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - currency_selection_sheet.dart contains: ScrollConfiguration(
    - currency_selection_sheet.dart contains: behavior: const ScrollBehavior()
    - currency_selection_sheet.dart contains: child: const CurrencySelectionSheet()
    - theme_selection_sheet.dart contains: ScrollConfiguration(
    - theme_selection_sheet.dart contains: child: const ThemeSelectionSheet()
    - language_selection_sheet.dart contains: ScrollConfiguration(
    - language_selection_sheet.dart contains: child: const LanguageSelectionSheet()
    - fvm flutter analyze lib/presentation/screens/settings/widgets/ exits 0 with no errors
  </acceptance_criteria>
  <done>All 3 settings selection sheets opt out of WeekletScrollBehavior scrollbar. All 8 modal sheets across the app now have the opt-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — pure UI layout and utility changes.

## STRIDE Threat Register

Not applicable — Phase 8 is pure UI layout and utility changes. No auth, session management, input validation of user-entered data, cryptography, or external data access is introduced or modified.
</threat_model>

<verification>
fvm flutter analyze lib/presentation/screens/expenses/widgets/list/ lib/presentation/screens/income/widgets/list/ lib/presentation/screens/categories/widgets/ lib/presentation/screens/settings/widgets/
</verification>

<success_criteria>
- All 6 remaining modal sheet builders wrapped with ScrollConfiguration(behavior: const ScrollBehavior())
- All 6 files pass fvm flutter analyze with no errors
- All 8 modal sheets across the app now opt out of WeekletScrollBehavior (completes D-11)
</success_criteria>

<output>
After completion, create .planning/phases/08-ui-polish/08-04-SUMMARY.md
</output>
