---
phase: 08-ui-polish
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/core/utils/number_formatter_test.dart
autonomous: true
requirements:
  - UX-04

must_haves:
  truths:
    - "test/core/utils/number_formatter_test.dart exists with 3 failing tests for formatCompact"
  artifacts:
    - path: "test/core/utils/number_formatter_test.dart"
      provides: "Failing unit tests for NumberFormatter.formatCompact (RED step)"
      contains: "formatCompact"
  key_links:
    - from: "test/core/utils/number_formatter_test.dart"
      to: "lib/core/utils/number_formatter.dart"
      via: "import"
      pattern: "import 'package:weeklet/core/utils/number_formatter.dart'"
---

<objective>
Create the test file for NumberFormatter.formatCompact before the implementation exists (RED step of TDD). This unblocks Plan 01 which implements formatCompact to make these tests green.

Purpose: Establish the contract for formatCompact via executable tests so Plan 01 knows exactly what to implement.
Output: test/core/utils/number_formatter_test.dart with 3 failing test cases.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/PROJECT.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/08-ui-polish/08-VALIDATION.md

<interfaces>
<!-- From lib/core/utils/number_formatter.dart — the class being tested -->
class NumberFormatter {
  NumberFormatter._();
  static String formatCurrency(double amount, String currencySymbol, {int decimals = 2, String locale = 'en_US'});
  static String formatCurrencyWithSign(double amount, String currencySymbol, {bool isIncome = false, int decimals = 2, String locale = 'en_US'});
  static String formatTime(DateTime dateTime);
  // formatCompact does NOT exist yet — tests will fail (RED)
}

<!-- Analog test file structure from test/core/constants/app_constants_test.dart -->
import 'package:flutter_test/flutter_test.dart';
// minimal unit test, no Flutter widgets, no setUpAll
void main() {
  group('...', () {
    test('...', () { expect(...); });
  });
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create number_formatter_test.dart (RED — tests fail)</name>
  <files>test/core/utils/number_formatter_test.dart</files>
  <read_first>
    - test/core/constants/app_constants_test.dart (analog structure — imports, group/test pattern)
    - lib/core/utils/number_formatter.dart (class under test — formatCurrency signature to understand delegation)
  </read_first>
  <behavior>
    - Test 1: formatCompact(1200.0, 'MDL') equals '1.2K MDL'
    - Test 2: formatCompact(-3500.0, 'EUR') equals '-3.5K EUR'
    - Test 3: formatCompact(999.99, 'MDL') equals '999.99 MDL' (delegates to formatCurrency)
  </behavior>
  <action>
Create the file test/core/utils/number_formatter_test.dart with this exact content:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:weeklet/core/utils/number_formatter.dart';

void main() {
  group('NumberFormatter.formatCompact (UX-04)', () {
    test('formats amount >= 1000 as compact K notation', () {
      expect(NumberFormatter.formatCompact(1200.0, 'MDL'), equals('1.2K MDL'));
    });

    test('formats negative amount >= 1000 with leading minus', () {
      expect(
        NumberFormatter.formatCompact(-3500.0, 'EUR'),
        equals('-3.5K EUR'),
      );
    });

    test('delegates to formatCurrency for amount < 1000', () {
      expect(
        NumberFormatter.formatCompact(999.99, 'MDL'),
        equals('999.99 MDL'),
      );
    });
  });
}
```

Run tests — they MUST fail (RED) because formatCompact does not exist yet. The test run will produce a compilation error or NoSuchMethodError. That is expected and correct.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/core/utils/number_formatter_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/core/utils/number_formatter_test.dart exists
    - File contains: import 'package:weeklet/core/utils/number_formatter.dart'
    - File contains: formatCompact(1200.0, 'MDL')
    - File contains: formatCompact(-3500.0, 'EUR')
    - File contains: formatCompact(999.99, 'MDL')
    - Running fvm flutter test test/core/utils/number_formatter_test.dart produces a failure (compilation error or test failure) — NOT a passing result
  </acceptance_criteria>
  <done>Test file exists with 3 test cases for formatCompact. Tests fail (RED) because implementation does not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — pure UI layout and utility changes.

## STRIDE Threat Register

Not applicable — Phase 8 is pure UI layout and utility changes. No auth, session management, input validation of user-entered data, cryptography, or external data access is introduced or modified.
</threat_model>

<verification>
fvm flutter test test/core/utils/number_formatter_test.dart
</verification>

<success_criteria>
test/core/utils/number_formatter_test.dart created with 3 failing tests for NumberFormatter.formatCompact.
</success_criteria>

<output>
After completion, create .planning/phases/08-ui-polish/08-00-SUMMARY.md
</output>
