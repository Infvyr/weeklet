---
phase: 08-ui-polish
plan: "01"
type: execute
wave: 1
depends_on:
  - "08-00"
files_modified:
  - lib/core/utils/number_formatter.dart
  - lib/core/utils/scroll_behavior.dart
autonomous: true
requirements:
  - UX-04
  - UX-05

must_haves:
  truths:
    - "NumberFormatter.formatCompact(1200.0, 'MDL') returns '1.2K MDL'"
    - "NumberFormatter.formatCompact(-3500.0, 'EUR') returns '-3.5K EUR'"
    - "NumberFormatter.formatCompact(999.99, 'MDL') returns '999.99 MDL'"
    - "WeekletScrollBehavior.buildScrollbar is overridden and returns a Scrollbar widget"
  artifacts:
    - path: "lib/core/utils/number_formatter.dart"
      provides: "formatCompact static method"
      contains: "formatCompact"
    - path: "lib/core/utils/scroll_behavior.dart"
      provides: "buildScrollbar override"
      contains: "buildScrollbar"
  key_links:
    - from: "test/core/utils/number_formatter_test.dart"
      to: "lib/core/utils/number_formatter.dart"
      via: "NumberFormatter.formatCompact"
      pattern: "static String formatCompact"
    - from: "lib/app.dart"
      to: "lib/core/utils/scroll_behavior.dart"
      via: "WeekletScrollBehavior — already wired globally"
      pattern: "WeekletScrollBehavior"
---

<objective>
Add formatCompact to NumberFormatter (UX-04) and buildScrollbar to WeekletScrollBehavior (UX-05). These are pure utility additions to existing classes — no new files, no new imports needed in either file. Plan 00 tests (RED) become GREEN after this plan completes.

Purpose: Provide the compact number formatter used by stats widgets, and the global scrollbar injection used by all scrollable screens.
Output: formatCompact static method in number_formatter.dart; buildScrollbar override in scroll_behavior.dart; test suite green.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/08-ui-polish/08-RESEARCH.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/08-ui-polish/08-PATTERNS.md

<interfaces>
<!-- lib/core/utils/number_formatter.dart — current state (read before editing) -->
import 'package:intl/intl.dart';

class NumberFormatter {
  NumberFormatter._();

  static String formatCurrency(double amount, String currencySymbol, {int decimals = 2, String locale = 'en_US'});
  // Produces: formatter.format(amount.abs()).trim() + ' ' + currencySymbol
  // Example: formatCurrency(999.99, 'MDL') -> "999.99 MDL"

  static String formatCurrencyWithSign(double amount, String currencySymbol, {bool isIncome = false, int decimals = 2, String locale = 'en_US'});
  static String formatTime(DateTime dateTime);
}

<!-- lib/core/utils/scroll_behavior.dart — current state (read before editing) -->
class WeekletScrollBehavior extends ScrollBehavior {
  @override
  Widget buildOverscrollIndicator(BuildContext context, Widget child, ScrollableDetails details) => child;

  @override
  ScrollPhysics getScrollPhysics(BuildContext context) => const BouncingScrollPhysics(parent: AlwaysScrollableScrollPhysics());
  // buildScrollbar is NOT overridden yet
}

<!-- Note: lib/app.dart already has MaterialApp.scrollBehavior: WeekletScrollBehavior()
     Adding buildScrollbar takes effect app-wide immediately — no app.dart change needed. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatCompact to NumberFormatter (GREEN)</name>
  <files>lib/core/utils/number_formatter.dart</files>
  <read_first>
    - lib/core/utils/number_formatter.dart (read full file — see current methods to position new method correctly)
    - test/core/utils/number_formatter_test.dart (read the 3 test cases — implement exactly what they expect)
  </read_first>
  <behavior>
    - formatCompact(1200.0, 'MDL') returns '1.2K MDL'
    - formatCompact(-3500.0, 'EUR') returns '-3.5K EUR'
    - formatCompact(999.99, 'MDL') returns '999.99 MDL' (delegates to formatCurrency)
    - formatCompact(0.0, 'MDL') returns '0.00 MDL' (delegates to formatCurrency — abs() is 0.0, below 1000)
    - formatCompact(-500.0, 'MDL') returns '-500.00 MDL' (delegates to formatCurrency — abs() is 500, below 1000)
  </behavior>
  <action>
Add the following static method to NumberFormatter in lib/core/utils/number_formatter.dart, placed after formatCurrencyWithSign and before formatTime. Follow the existing doc comment style with /// and dart code fence:

```dart
/// Formats a currency amount compactly — values >= 1000 are abbreviated.
///
/// Example:
/// ```dart
/// formatCompact(1200.0, 'MDL')   // "1.2K MDL"
/// formatCompact(-3500.0, 'EUR')  // "-3.5K EUR"
/// formatCompact(999.99, 'MDL')   // "999.99 MDL"
/// ```
static String formatCompact(double amount, String currencySymbol) {
  if (amount.abs() >= 1000) {
    final sign = amount < 0 ? '-' : '';
    final compact = (amount.abs() / 1000).toStringAsFixed(1);
    return '${sign}${compact}K $currencySymbol';
  }
  return formatCurrency(amount, currencySymbol);
}
```

Key implementation notes:
- Use amount.abs() for the >= 1000 check so negative values like -3500 also abbreviate
- Compute sign separately: amount < 0 ? '-' : ''
- Use (amount.abs() / 1000).toStringAsFixed(1) — NOT (amount / 1000) which would give "-3.5" and break the sign logic
- Delegate to formatCurrency for amounts below 1000 — this preserves the existing locale-aware formatting (en_US NumberFormat)
- No new imports needed — uses only Dart core (toStringAsFixed is on double)

After adding the method, run the tests to verify GREEN:
fvm flutter test test/core/utils/number_formatter_test.dart
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/core/utils/number_formatter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/utils/number_formatter.dart contains: static String formatCompact(double amount, String currencySymbol)
    - File contains: amount.abs() >= 1000
    - File contains: toStringAsFixed(1)
    - File contains: return formatCurrency(amount, currencySymbol);
    - fvm flutter test test/core/utils/number_formatter_test.dart exits 0 with all 3 tests passing
  </acceptance_criteria>
  <done>formatCompact is implemented, all 3 tests pass (GREEN).</done>
</task>

<task type="auto">
  <name>Task 2: Add buildScrollbar override to WeekletScrollBehavior</name>
  <files>lib/core/utils/scroll_behavior.dart</files>
  <read_first>
    - lib/core/utils/scroll_behavior.dart (read full file — copy the arrow body method style from existing overrides)
  </read_first>
  <action>
Add the following override to WeekletScrollBehavior in lib/core/utils/scroll_behavior.dart, placed after the getScrollPhysics method. Use the same arrow body (=>) style as the two existing overrides:

```dart
@override
Widget buildScrollbar(
  BuildContext context,
  Widget child,
  ScrollableDetails details,
) => Scrollbar(
  controller: details.controller,
  thumbVisibility: false,
  child: child,
);
```

Key notes:
- thumbVisibility: false — scrollbar thumb appears only during active scroll, fades when idle. Do NOT use thumbVisibility: true (causes always-visible track on non-scrollable short lists).
- controller: details.controller — connects the scrollbar to the scrollable's controller
- No new imports needed — Scrollbar and ScrollableDetails are in package:flutter/material.dart which is already imported
- The MaterialApp.scrollBehavior: WeekletScrollBehavior() in lib/app.dart already wires this globally — no changes to app.dart required

After adding, run the full core test suite to verify no regressions:
fvm flutter test test/core/
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/core/</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/utils/scroll_behavior.dart contains: Widget buildScrollbar(
    - File contains: thumbVisibility: false
    - File contains: controller: details.controller
    - fvm flutter test test/core/ exits 0 with all tests passing
    - grep result: lib/core/utils/scroll_behavior.dart contains exactly 3 @override annotations (buildOverscrollIndicator, getScrollPhysics, buildScrollbar)
  </acceptance_criteria>
  <done>buildScrollbar override added. Global scrollbar is active app-wide via existing WeekletScrollBehavior wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — pure UI layout and utility changes.

## STRIDE Threat Register

Not applicable — Phase 8 is pure UI layout and utility changes. No auth, session management, input validation of user-entered data, cryptography, or external data access is introduced or modified.
</threat_model>

<verification>
fvm flutter test test/core/
</verification>

<success_criteria>
- All 3 formatCompact tests pass (GREEN)
- WeekletScrollBehavior has buildScrollbar override with thumbVisibility: false
- fvm flutter test test/core/ exits 0
</success_criteria>

<output>
After completion, create .planning/phases/08-ui-polish/08-01-SUMMARY.md
</output>
