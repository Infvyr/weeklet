---
phase: 12-widget-component-tests
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - test/presentation/screens/income/income_screen_test.dart
  - test/presentation/screens/categories/categories_screen_test.dart
autonomous: true
requirements: [TEST-18, TEST-19]

must_haves:
  truths:
    - "IncomeScreen shows CircularProgressIndicator when state is IncomeLoading"
    - "IncomeScreen shows IncomeListView (or equivalent) when filteredIncomes is non-empty"
    - "CategoriesScreen shows CircularProgressIndicator when state is CategoryLoading"
    - "CategoriesScreen shows category items grid when state is CategoriesLoaded with categories"
    - "CategoriesScreen shows empty-categories view when CategoriesLoaded with empty list"
  artifacts:
    - path: "test/presentation/screens/income/income_screen_test.dart"
      provides: "Widget tests for TEST-18 (loading, list, empty-list states)"
      exports: ["main"]
    - path: "test/presentation/screens/categories/categories_screen_test.dart"
      provides: "Widget tests for TEST-19 (loading, loaded-with-items, loaded-empty states)"
      exports: ["main"]
  key_links:
    - from: "test/presentation/screens/income/income_screen_test.dart"
      to: "lib/presentation/screens/income/income_screen.dart"
      via: "testWidgets + pumpApp"
      pattern: "IncomeScreen"
    - from: "test/presentation/screens/categories/categories_screen_test.dart"
      to: "lib/presentation/screens/categories/categories_screen.dart"
      via: "testWidgets + pumpApp + isA<> matchers"
      pattern: "CategoriesScreen"
---

<objective>
Write testWidgets-based tests for IncomeScreen (TEST-18) and CategoriesScreen (TEST-19). IncomeScreen mirrors the ExpensesScreen pattern but uses IncomeBloc instead of ExpenseBloc and does NOT use CategoryBloc. CategoriesScreen is the simplest screen test (only CategoryBloc), but requires isA<> matchers because CategoryState does NOT extend Equatable.

Purpose: Verify that IncomeScreen correctly renders per IncomeBloc state. Verify that CategoriesScreen renders the correct tree for CategoryLoading, CategoriesLoaded-with-items, and CategoriesLoaded-empty.

Output: Two test files covering TEST-18 and TEST-19.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-CONTEXT.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-RESEARCH.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-PATTERNS.md

@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Read source files before writing tests. -->

From lib/presentation/screens/income/income_screen.dart:
  - BLoC dependencies: IncomeBloc, SettingsBloc, ExportBloc (has BlocListener<ExportBloc>)
  - No CategoryBloc dependency — do NOT include MockCategoryBloc
  - No sl<> calls in initState (initState only sets up ScrollController)
  - IncomeLoading → CircularProgressIndicator
  - IncomeSuccess with filteredIncomes.isEmpty → IncomeListView with empty list (NOT EmptyStateView — income screen does not use EmptyStateView widget)
  - IncomeSuccess with filteredIncomes non-empty → IncomeListView rendered

From lib/presentation/screens/categories/categories_screen.dart:
  - BLoC dependencies: CategoryBloc ONLY (no ExportBloc, no SettingsBloc)
  - No sl<> calls in initState
  - CategoryLoading → CircularProgressIndicator
  - CategoriesLoaded with empty list → CategoriesEmptyView (or equivalent; read source to confirm class name)
  - CategoriesLoaded with items → GridView or CategoryItemView widgets
  - CategoryError → Center with error text

CategoryState/Event NOT Equatable — critical:
  - Do NOT use equality matchers: expect(state, CategoriesLoaded(...)) FAILS
  - DO use: when(() => mockCategoryBloc.state).thenReturn(CategoriesLoaded(categories: [...]))
  - DO use: isA<CategoriesLoaded>() in matchers if needed
  - CategoryBloc initial state is CategoryLoading() — no CategoryInitial class exists

IncomeSuccess constructor (from income_state.dart):
  IncomeSuccess({ required List<Income> allIncomes, required List<Income> filteredIncomes,
                  required int selectedYear, required List<int> availableYears,
                  required List<int> availableMonths, int? selectedMonth, String? actionError })

Import paths for income_screen_test.dart:
  import 'package:weeklet/presentation/blocs/income/income_bloc.dart';
  import 'package:weeklet/presentation/blocs/income/income_event.dart';
  import 'package:weeklet/presentation/blocs/income/income_state.dart';
  import 'package:weeklet/presentation/blocs/export/export_bloc.dart';
  import 'package:weeklet/presentation/blocs/export/export_event.dart';
  import 'package:weeklet/presentation/blocs/export/export_state.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_bloc.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_event.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_state.dart';
  import 'package:weeklet/presentation/screens/income/income_screen.dart';
  import '../../../../helpers/test_app.dart';
  import '../../../../helpers/test_data.dart';

Import paths for categories_screen_test.dart:
  import 'package:weeklet/presentation/blocs/category/category_bloc.dart';
  import 'package:weeklet/presentation/blocs/category/category_event.dart';
  import 'package:weeklet/presentation/blocs/category/category_state.dart';
  import 'package:weeklet/presentation/screens/categories/categories_screen.dart';
  import '../../../../helpers/test_app.dart';
  import '../../../../helpers/test_data.dart';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write income_screen_test.dart (TEST-18)</name>
  <files>test/presentation/screens/income/income_screen_test.dart</files>
  <read_first>
    - lib/presentation/screens/income/income_screen.dart — read fully to confirm: exact BLoC imports, BlocListener/BlocBuilder usage, widget types rendered per state (especially what renders for IncomeSuccess with empty vs non-empty filteredIncomes — does it render EmptyStateView or IncomeListView?)
    - lib/presentation/blocs/income/income_state.dart — confirm IncomeSuccess, IncomeLoading, IncomeFailure class names and IncomeSuccess constructor params
    - lib/presentation/blocs/income/income_event.dart — confirm LoadIncomesRequested event class name for event dispatch test
    - lib/presentation/blocs/export/export_state.dart — confirm ExportInitial class name
    - test/helpers/test_data.dart — confirm fakeIncomeSuccess factory signature (created by Plan 01)
  </read_first>
  <action>
    Create `test/presentation/screens/income/income_screen_test.dart` following the same structural pattern as expenses_screen_test.dart from Plan 02, adapted for IncomeBloc, SettingsBloc, ExportBloc (no CategoryBloc).

    Mock classes: MockIncomeBloc (MockBloc<IncomeEvent, IncomeState>), MockSettingsBloc (MockBloc<SettingsEvent, SettingsState>), MockExportBloc (MockBloc<ExportEvent, ExportState>). Fake classes: FakeIncomeEvent, FakeIncomeState, FakeSettingsEvent, FakeSettingsState, FakeExportEvent, FakeExportState.

    setUpAll: registerFallbackValue for each Fake class instance.

    setUp: instantiate 3 mocks; seed with when().thenReturn() and whenListen() for each: IncomeLoading(), SettingsLoaded(settings: Settings.defaults()), ExportInitial(); register all 3 in GetIt.instance.

    tearDown: await GetIt.instance.reset().

    Helper `_pumpIncomeScreen(WidgetTester tester)`: calls pumpApp with const IncomeScreen() and providers for all 3 BLoCs.

    Test group 'IncomeScreen':
    - 'shows CircularProgressIndicator when state is IncomeLoading': pump with default seed, await tester.pump(), expect find.byType(CircularProgressIndicator).findsOneWidget
    - 'shows income list when filteredIncomes is non-empty': override mockIncomeBloc state to fakeIncomeSuccess(incomes: [fakeIncome()]); pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing (income items are rendered, not loading indicator)
    - 'shows no CircularProgressIndicator when filteredIncomes is empty (IncomeSuccess)': override mockIncomeBloc state to fakeIncomeSuccess(incomes: []); pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing (IncomeListView renders even for empty list per RESEARCH.md notes)

    Note from RESEARCH.md Pattern for IncomeScreen: IncomeScreen does NOT render EmptyStateView for empty list — it renders IncomeListView regardless. Verify this by reading the screen source. Adjust tests accordingly.

    Per D-02: add one event dispatch test — find the year filter or a button that dispatches a LoadIncomesRequested or FilterDateChanged; tap it; verify the event. If no easily tappable element dispatches a testable event in the default loading state, seed an IncomeSuccess state and test pull-to-refresh or a year chip tap.

    Use single quotes, trailing commas, const constructors. Run `fvm flutter analyze test/presentation/screens/income/income_screen_test.dart --no-pub` and fix issues before commit.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/presentation/screens/income/income_screen_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/presentation/screens/income/income_screen_test.dart exists
    - File contains at minimum 3 testWidgets cases
    - `fvm flutter test test/presentation/screens/income/income_screen_test.dart --no-pub` exits 0 with all tests passing
    - `fvm flutter analyze test/presentation/screens/income/income_screen_test.dart --no-pub` exits 0 with 0 issues
    - No MockCategoryBloc in this file (IncomeScreen does not use CategoryBloc)
    - tearDown calls GetIt.instance.reset()
  </acceptance_criteria>
  <done>income_screen_test.dart exists with all tests passing and 0 lint issues</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write categories_screen_test.dart (TEST-19)</name>
  <files>test/presentation/screens/categories/categories_screen_test.dart</files>
  <read_first>
    - lib/presentation/screens/categories/categories_screen.dart — read fully to confirm: only CategoryBloc is used, exact widget types for CategoryLoading/CategoriesLoaded/CategoryError states (especially the empty-state widget class name and the list widget class name)
    - lib/presentation/blocs/category/category_state.dart — confirm that CategoryState is a sealed class with NO extends Equatable; confirm CategoriesLoaded(categories: List<Category>) constructor; confirm CategoryLoading() is the initial state; confirm CategoriesEmptyView or whatever the empty-state widget is (read categories_screen.dart for this)
    - lib/presentation/blocs/category/category_event.dart — confirm GetAllCategoriesEvent class name (needed for any event dispatch verify)
    - test/helpers/test_data.dart — confirm fakeCategory factory
  </read_first>
  <action>
    Create `test/presentation/screens/categories/categories_screen_test.dart` following the MockBloc pattern but adapted for CategoryBloc only.

    Mock classes: MockCategoryBloc (MockBloc<CategoryEvent, CategoryState>). Fake classes: FakeCategoryEvent (Fake implements CategoryEvent), FakeCategoryState (Fake implements CategoryState).

    CRITICAL: CategoryState does NOT extend Equatable. Do NOT use equality matchers. Use when(() => mockCategoryBloc.state).thenReturn(stateInstance) to control state. CategoriesLoaded is the loaded state. CategoryLoading() is the initial state.

    setUpAll: registerFallbackValue(FakeCategoryEvent()); registerFallbackValue(FakeCategoryState()).

    setUp: mockCategoryBloc = MockCategoryBloc(); when(() => mockCategoryBloc.state).thenReturn(const CategoryLoading()); whenListen(mockCategoryBloc, const Stream<CategoryState>.empty(), initialState: const CategoryLoading()); GetIt.instance.registerSingleton<CategoryBloc>(mockCategoryBloc).

    tearDown: await GetIt.instance.reset().

    Helper `_pumpCategoriesScreen(WidgetTester tester)`: pumpApp with const CategoriesScreen() (or the actual screen class — read source to get class name) and [BlocProvider<CategoryBloc>.value(value: mockCategoryBloc)].

    Test group 'CategoriesScreen':
    - 'shows CircularProgressIndicator when state is CategoryLoading': default seed, pump, await tester.pump(), expect find.byType(CircularProgressIndicator).findsOneWidget
    - 'shows category grid when CategoriesLoaded with categories': override state to CategoriesLoaded(categories: [fakeCategory()]); call whenListen with CategoriesLoaded stream; pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing and expect category items to be visible (use find.text('Food') or find.byType(GridView) or the CategoryItemView widget type — read source to know which)
    - 'shows empty state when CategoriesLoaded with empty list': override state to const CategoriesLoaded(categories: []); pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing; expect the empty-state widget (read categories_screen.dart for the class name — it may be CategoriesEmptyView or a Column with specific text)

    Note on isA<> matchers: Since CategoryState is NOT Equatable, if any assertion compares state instances, use isA<CategoriesLoaded>() rather than equality. For widget finding, normal find.byType() is fine.

    CategoriesScreen FAB navigates (not a BLoC event dispatch) — no event dispatch test needed for FAB per PATTERNS.md.

    Use single quotes, trailing commas, const constructors. Run `fvm flutter analyze test/presentation/screens/categories/categories_screen_test.dart --no-pub` and fix issues.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/presentation/screens/categories/categories_screen_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/presentation/screens/categories/categories_screen_test.dart exists
    - File contains at minimum 3 testWidgets cases (loading, loaded-with-items, loaded-empty)
    - `fvm flutter test test/presentation/screens/categories/categories_screen_test.dart --no-pub` exits 0 with all tests passing
    - `fvm flutter analyze test/presentation/screens/categories/categories_screen_test.dart --no-pub` exits 0 with 0 issues
    - File does NOT use equality matchers for CategoryState (no expect(state, CategoriesLoaded(...)) pattern)
    - File uses when(() => mockCategoryBloc.state).thenReturn(...) to set state per test
    - tearDown calls GetIt.instance.reset()
  </acceptance_criteria>
  <done>categories_screen_test.dart exists with all tests passing and 0 lint issues; no Equatable equality matchers used for CategoryState</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | Plan 03 writes test files only; no production code is modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | N/A | All STRIDE categories | accept | No production code written. Test files only. No ASVS L1 categories apply to test infrastructure. |
</threat_model>

<verification>
After both tasks complete:
- `fvm flutter test test/presentation/screens/income/income_screen_test.dart --no-pub` exits 0
- `fvm flutter test test/presentation/screens/categories/categories_screen_test.dart --no-pub` exits 0
- `fvm flutter test --no-pub` exits 0 (all tests passing, no regressions)
- `fvm flutter analyze test/presentation/screens/income/ test/presentation/screens/categories/ --no-pub` exits 0
</verification>

<success_criteria>
- income_screen_test.dart: 3+ tests covering IncomeLoading, IncomeSuccess states; MockIncomeBloc, MockSettingsBloc, MockExportBloc only (no CategoryBloc)
- categories_screen_test.dart: 3+ tests covering CategoryLoading, CategoriesLoaded-with-items, CategoriesLoaded-empty; uses when() not equality matchers
- fvm flutter test --no-pub exits 0 (no regressions)
</success_criteria>

<output>
Create `.planning/phases/12-widget-component-tests/12-03-SUMMARY.md` when done.
</output>
