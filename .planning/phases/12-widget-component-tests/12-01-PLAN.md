---
phase: 12-widget-component-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - test/helpers/test_app.dart
  - test/helpers/test_data.dart
autonomous: true
requirements: [TEST-16, TEST-17, TEST-18, TEST-19, TEST-20, TEST-21, TEST-22, TEST-23]

must_haves:
  truths:
    - "All 8 widget test files can import pumpApp from test/helpers/test_app.dart"
    - "All 8 widget test files can import fakeExpense, fakeCategory, fakeIncome, fakeMonthlyStatsLoaded from test/helpers/test_data.dart"
    - "mocktail is a direct dev_dependency in pubspec.yaml so `import 'package:mocktail/mocktail.dart'` resolves"
    - "fvm flutter pub get exits 0 after pubspec change"
  artifacts:
    - path: "test/helpers/test_app.dart"
      provides: "pumpApp(tester, child, {providers}) helper with localization + MultiBlocProvider"
      exports: ["pumpApp"]
    - path: "test/helpers/test_data.dart"
      provides: "Shared entity factories reusable across all 8 screen test files"
      exports: ["fakeExpense", "fakeCategory", "fakeIncome", "fakeMonthlyStatsLoaded", "fakeExpenseSuccess", "fakeIncomeSuccess"]
    - path: "pubspec.yaml"
      provides: "Direct dev_dependency on mocktail ^1.0.5"
      contains: "mocktail: ^1.0.5"
  key_links:
    - from: "test/presentation/screens/*/screen_test.dart"
      to: "test/helpers/test_app.dart"
      via: "import '../../../helpers/test_app.dart'"
      pattern: "pumpApp"
    - from: "test/presentation/screens/*/screen_test.dart"
      to: "test/helpers/test_data.dart"
      via: "import '../../../helpers/test_data.dart'"
      pattern: "fakeExpense|fakeCategory|fakeIncome"
---

<objective>
Create the shared widget-test foundation that all 8 subsequent screen test files depend on: elevate mocktail to a direct dev_dependency, create the pumpApp helper that wraps any widget in MaterialApp + localization + MultiBlocProvider, and create the shared test data factory file.

Purpose: Without this foundation, every screen test file would need to duplicate 20+ lines of localization boilerplate and construct entity fixtures independently. pumpApp is the single point of truth for the localization wrapper; test_data.dart is the single point of truth for fake entities.

Output: pubspec.yaml with mocktail direct dep; test/helpers/test_app.dart; test/helpers/test_data.dart.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-CONTEXT.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-RESEARCH.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-PATTERNS.md

<!-- Cross-phase patterns -->
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/test/helpers/fake_blocs.dart
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/test/presentation/blocs/expense/expense_bloc_test.dart

<interfaces>
<!-- Key types the executor needs to create factories for. -->

From lib/domain/entities/expense.dart (confirmed field set):
  Expense({ required String id, required double amount, required String description,
             required String categoryId, required DateTime createdAt })

From lib/domain/entities/category.dart:
  Category({ required String id, required String name, required String icon,
              required DateTime createdAt })

From lib/domain/entities/income.dart:
  Income({ required String id, required double amount, required String description,
           required DateTime date, required DateTime createdAt })

From lib/presentation/blocs/expense/expense_state.dart (ExpenseSuccess):
  ExpenseSuccess({ required List<Expense> allExpenses, required List<Expense> filteredExpenses,
                   required int selectedYear, required List<int> availableYears,
                   required List<int> availableMonths, int? selectedMonth, String? actionError })

From lib/presentation/blocs/income/income_state.dart (IncomeSuccess):
  IncomeSuccess({ required List<Income> allIncomes, required List<Income> filteredIncomes,
                  required int selectedYear, required List<int> availableYears,
                  required List<int> availableMonths, int? selectedMonth, String? actionError })

From lib/presentation/blocs/stats/stats_state.dart (MonthlyStatsLoaded):
  MonthlyStatsLoaded({ required MonthlyStats stats, required int year,
                       int? month, EvolutionStats? evolutionStats, ... })

From lib/domain/entities/statistics.dart (MonthlyStats):
  MonthlyStats({ required double totalIncome, required double totalExpenses,
                 required double balance, required double incomeGrowthPercentage,
                 required double expenseGrowthPercentage, required List<CategoryStats> categoryStats })

From lib/domain/entities/settings.dart:
  Settings.defaults() — static factory returning default Settings object

Localization import path (confirmed from lib/app.dart):
  import 'package:weeklet/l10n/app_localizations.dart';
  AppLocalizations.localizationsDelegates
  AppLocalizations.supportedLocales
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add mocktail to pubspec.yaml dev_dependencies</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml — read the current dev_dependencies block to know where to insert
  </read_first>
  <action>
    Add `mocktail: ^1.0.5` as a direct dev_dependency in pubspec.yaml. Insert it after the `bloc_test: ^10.0.0` line in the dev_dependencies block. Then run `fvm flutter pub get` to resolve and verify the addition. mocktail 1.0.5 is already resolved as a transitive dep of bloc_test (confirmed in pubspec.lock); elevating it to a direct dep pins the version and makes `import 'package:mocktail/mocktail.dart'` resolve without relying on transitive availability (per D-03).
  </action>
  <verify>
    <automated>grep 'mocktail:' /Users/vasilenovatchii/My\ Projects/Flutter/Weeklet/pubspec.yaml</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml dev_dependencies block contains `mocktail: ^1.0.5` (exact version constraint)
    - `fvm flutter pub get --no-pub` exits 0 (or `fvm flutter pub get` exits 0)
    - pubspec.lock still resolves mocktail at 1.0.5 (version did not change)
  </acceptance_criteria>
  <done>mocktail appears as a direct entry in dev_dependencies; `fvm flutter pub get` succeeds</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test/helpers/test_app.dart and test/helpers/test_data.dart</name>
  <files>test/helpers/test_app.dart, test/helpers/test_data.dart</files>
  <read_first>
    - lib/app.dart — verify AppLocalizations import path and MultiBlocProvider provider list pattern
    - lib/domain/entities/expense.dart — confirm Expense constructor field names and types
    - lib/domain/entities/income.dart — confirm Income has both `date` and `createdAt` fields
    - lib/domain/entities/category.dart — confirm Category field names
    - lib/domain/entities/settings.dart — confirm Settings.defaults() factory exists
    - lib/domain/entities/statistics.dart — confirm MonthlyStats and CategoryStats constructors
    - lib/presentation/blocs/expense/expense_state.dart — confirm ExpenseSuccess constructor params
    - lib/presentation/blocs/income/income_state.dart — confirm IncomeSuccess constructor params
    - lib/presentation/blocs/stats/stats_state.dart — confirm MonthlyStatsLoaded constructor params
    - test/presentation/blocs/expense/expense_bloc_test.dart — read the _fakeExpense/_successSeed helper pattern (lines 76-94) to match exactly
  </read_first>
  <action>
    Create `test/helpers/test_app.dart` implementing the `pumpApp` helper (per D-05). The function signature is:
      `Future<void> pumpApp(WidgetTester tester, Widget child, {List<BlocProvider> providers = const []}) async`
    It calls `tester.pumpWidget` with `MultiBlocProvider` as the outer widget (providers from parameter) wrapping `MaterialApp` with `localizationsDelegates: AppLocalizations.localizationsDelegates`, `supportedLocales: AppLocalizations.supportedLocales`, `locale: const Locale('en')`, `home: child`. The `MaterialApp` is the child of `MultiBlocProvider`, not the outer wrapper — this ensures BlocProvider values are available in the widget tree including MaterialApp descendants.

    Create `test/helpers/test_data.dart` with these top-level (public, no `_` prefix) factory functions — they must be importable from other test files:
    - `Expense fakeExpense({String id = 'exp-1', double amount = 100.0, String categoryId = 'cat-1'})` — constructs Expense with fixed values; `description = 'Test expense'`; `createdAt = DateTime(DateTime.now().year, 5, 1)`
    - `Category fakeCategory({String id = 'cat-1', String name = 'Food'})` — constructs Category with `icon = 'fastfood'`; `createdAt = DateTime(2025, 1, 1)`
    - `Income fakeIncome({String id = 'inc-1', double amount = 500.0})` — constructs Income with `description = 'Salary'`; `date = DateTime(DateTime.now().year, 5, 1)`; `createdAt = DateTime(DateTime.now().year, 5, 1)`
    - `ExpenseSuccess fakeExpenseSuccess({List<Expense>? expenses})` — builds ExpenseSuccess with `allExpenses` and `filteredExpenses` both set to `expenses ?? [fakeExpense()]`; `selectedYear = DateTime.now().year`; `selectedMonth = null`; `availableYears = [DateTime.now().year]`; `availableMonths = const [5]`
    - `IncomeSuccess fakeIncomeSuccess({List<Income>? incomes})` — mirrors fakeExpenseSuccess pattern for IncomeSuccess
    - `MonthlyStatsLoaded fakeMonthlyStatsLoaded()` — builds MonthlyStatsLoaded with MonthlyStats having `totalIncome: 1000.0`, `totalExpenses: 500.0`, `balance: 500.0`, all growth percentages `0.0`, `categoryStats: const []`; `year = DateTime.now().year`

    All files use single quotes, trailing commas, and const constructors wherever possible (matching project lint rules). Import `package:weeklet/...` paths for all production code imports. Import `package:flutter_bloc/flutter_bloc.dart` for BlocProvider type in test_app.dart. Import `package:weeklet/l10n/app_localizations.dart` in test_app.dart.

    After creating both files, run `fvm flutter analyze test/helpers/ --no-pub` to confirm 0 lint issues.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter analyze test/helpers/ --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/helpers/test_app.dart exists and exports the `pumpApp` top-level function
    - test/helpers/test_data.dart exists and exports fakeExpense, fakeCategory, fakeIncome, fakeExpenseSuccess, fakeIncomeSuccess, fakeMonthlyStatsLoaded as top-level functions (not private)
    - `fvm flutter analyze test/helpers/ --no-pub` reports 0 issues (no lint errors, no type errors)
    - pumpApp wraps child in MultiBlocProvider(providers: providers, child: MaterialApp(localizationsDelegates: AppLocalizations.localizationsDelegates, ...))
    - fakeExpense, fakeCategory, fakeIncome have named optional parameters with defaults so callers can override id and other fields
    - fakeExpenseSuccess and fakeIncomeSuccess accept an optional `expenses`/`incomes` list parameter
  </acceptance_criteria>
  <done>Both helper files exist with 0 analyzer issues; all factory functions are public top-level functions with correct signatures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | Phase 12 writes test files only; no production code paths are modified except pubspec.yaml dev_dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | mocktail package (pub.dev) | accept | mocktail 1.0.5 is already resolved as a transitive dep of bloc_test in pubspec.lock, authored by Felix Angell (bloc ecosystem author), from official pub.dev registry. Elevating from transitive to direct dep at same version introduces no new attack surface. |
| T-12-01 | N/A | All other STRIDE categories | accept | No production code written. No auth, session, access control, crypto, input handling, or data paths added. No ASVS L1 categories apply to test infrastructure. |
</threat_model>

<verification>
After both tasks complete:
- `grep 'mocktail:' pubspec.yaml` shows `mocktail: ^1.0.5` in dev_dependencies
- `fvm flutter pub get` exits 0
- `fvm flutter analyze test/helpers/ --no-pub` exits 0 with no issues
- `fvm flutter test --no-pub` exits 0 (all 130 prior tests still pass; no regressions from pubspec change)
</verification>

<success_criteria>
- pubspec.yaml contains `mocktail: ^1.0.5` as a direct dev_dependency
- test/helpers/test_app.dart provides `pumpApp` with MaterialApp + AppLocalizations delegates + MultiBlocProvider
- test/helpers/test_data.dart provides 6 public factory functions for fake entities and states
- fvm flutter test --no-pub exits 0 (130 tests, no regressions)
- fvm flutter analyze test/helpers/ --no-pub exits 0
</success_criteria>

<output>
Create `.planning/phases/12-widget-component-tests/12-01-SUMMARY.md` when done.
</output>
