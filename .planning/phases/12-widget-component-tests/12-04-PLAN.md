---
phase: 12-widget-component-tests
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - test/presentation/screens/stats/stats_screen_test.dart
  - test/presentation/screens/settings/settings_screen_test.dart
autonomous: true
requirements: [TEST-20, TEST-21]

must_haves:
  truths:
    - "StatsScreen shows CircularProgressIndicator when state is StatsInitial or StatsLoading"
    - "StatsScreen renders chart widgets when state is MonthlyStatsLoaded with MonthlyStats data"
    - "SettingsScreen shows CircularProgressIndicator when state is SettingsInitial or SettingsLoading"
    - "SettingsScreen renders settings list when state is SettingsLoaded"
    - "SettingsScreen dispatches BiometricToggled when biometric switch is tapped"
  artifacts:
    - path: "test/presentation/screens/stats/stats_screen_test.dart"
      provides: "Widget tests for TEST-20 (loading, MonthlyStatsLoaded chart rendering)"
      exports: ["main"]
    - path: "test/presentation/screens/settings/settings_screen_test.dart"
      provides: "Widget tests for TEST-21 (loading, SettingsLoaded toggle interactions)"
      exports: ["main"]
  key_links:
    - from: "test/presentation/screens/stats/stats_screen_test.dart"
      to: "lib/presentation/screens/stats/stats_screen.dart"
      via: "testWidgets + pumpApp"
      pattern: "MonthlyStatsLoaded"
    - from: "test/presentation/screens/settings/settings_screen_test.dart"
      to: "lib/presentation/screens/settings/settings_screen.dart"
      via: "testWidgets + pumpApp + PackageInfo.setMockInitialValues"
      pattern: "BiometricToggled"
---

<objective>
Write testWidgets-based tests for StatsScreen (TEST-20) and SettingsScreen (TEST-21). StatsScreen is a StatelessWidget reading StatsBloc + SettingsBloc — the simplest screen test structurally. SettingsScreen requires a PackageInfo platform channel stub (setMockInitialValues) in setUpAll and tests that toggle interactions dispatch the correct events (D-02 event dispatch verification).

Purpose: Verify StatsScreen renders chart widgets when MonthlyStatsLoaded is seeded. Verify SettingsScreen renders settings list when SettingsLoaded and dispatches correct events (BiometricToggled) on toggle interactions.

Output: Two test files covering TEST-20 and TEST-21.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-CONTEXT.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-RESEARCH.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-PATTERNS.md

@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-01-SUMMARY.md

<!-- Phase 11 settings test for consistency -->
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/test/presentation/blocs/settings/settings_bloc_test.dart

<interfaces>
<!-- Key contracts the executor needs. Read source files before writing tests. -->

From lib/presentation/screens/stats/stats_screen.dart:
  - StatsScreen is a StatelessWidget — no initState, no sl<> calls
  - BLoC dependencies: StatsBloc (BlocBuilder) + SettingsBloc (reads currencySymbol)
  - StatsInitial or StatsLoading → CircularProgressIndicator (or StatsLoadingView)
  - StatsError → StatsErrorView
  - MonthlyStatsLoaded → CustomScrollView with chart/stat widgets

From lib/presentation/screens/settings/settings_screen.dart:
  - BLoC dependencies: SettingsBloc only
  - initState calls PackageInfo.fromPlatform() → MUST call PackageInfo.setMockInitialValues in setUpAll
  - SettingsInitial or SettingsLoading → CircularProgressIndicator
  - SettingsLoaded → ListView with Security, Appearance, Data, Legal, About sections
  - Biometric Switch.adaptive dispatches BiometricToggled(enabled: bool)
  - ThemeChanged, LocaleChanged, CurrencyChanged events dispatched by other toggle/selector widgets

MonthlyStatsLoaded constructor (from stats_state.dart):
  MonthlyStatsLoaded({ required MonthlyStats stats, required int year,
                        int? month, EvolutionStats? evolutionStats,
                        Map<int, List<int>>? availablePeriods, int? currentTab,
                        int touchedIndex = -1 })

SettingsLoaded constructor (from settings_state.dart):
  SettingsLoaded({ required Settings settings, String? actionError })

Settings.defaults() — provides a default Settings object for test seeding

BiometricToggled constructor (from settings_event.dart):
  BiometricToggled({ required bool enabled })

PackageInfo stub (per D-09 research + package_info_plus 10.0.0 API):
  import 'package:package_info_plus/package_info_plus.dart';
  setUpAll(() {
    PackageInfo.setMockInitialValues(
      appName: 'Weeklet', packageName: 'com.weeklet.app',
      version: '1.0.0', buildNumber: '1', buildSignature: '',
    );
  });

Import paths for stats_screen_test.dart:
  import 'package:weeklet/domain/entities/settings.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_bloc.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_event.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_state.dart';
  import 'package:weeklet/presentation/blocs/stats/stats_bloc.dart';
  import 'package:weeklet/presentation/blocs/stats/stats_event.dart';
  import 'package:weeklet/presentation/blocs/stats/stats_state.dart';
  import 'package:weeklet/presentation/screens/stats/stats_screen.dart';
  import '../../../../helpers/test_app.dart';
  import '../../../../helpers/test_data.dart';

Import paths for settings_screen_test.dart:
  import 'package:package_info_plus/package_info_plus.dart';
  import 'package:weeklet/domain/entities/settings.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_bloc.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_event.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_state.dart';
  import 'package:weeklet/presentation/screens/settings/settings_screen.dart';
  import '../../../../helpers/test_app.dart';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write stats_screen_test.dart (TEST-20)</name>
  <files>test/presentation/screens/stats/stats_screen_test.dart</files>
  <read_first>
    - lib/presentation/screens/stats/stats_screen.dart — read fully to confirm: StatsBloc + SettingsBloc as the only BLoC deps; exact widget types rendered for StatsInitial/StatsLoading vs MonthlyStatsLoaded; confirm StatelessWidget (no initState, no sl<> calls)
    - lib/presentation/blocs/stats/stats_state.dart — confirm StatsInitial, StatsLoading, MonthlyStatsLoaded class names; confirm MonthlyStatsLoaded constructor params (especially optional fields like evolutionStats, availablePeriods)
    - lib/presentation/blocs/stats/stats_event.dart — confirm LoadMonthlyStats event class for any event dispatch test
    - lib/domain/entities/statistics.dart — confirm MonthlyStats and CategoryStats constructors (needed for fakeMonthlyStatsLoaded or manual construction)
    - test/helpers/test_data.dart — confirm fakeMonthlyStatsLoaded factory
    - test/helpers/test_app.dart — confirm pumpApp signature
  </read_first>
  <action>
    Create `test/presentation/screens/stats/stats_screen_test.dart` with MockStatsBloc and MockSettingsBloc pattern.

    Mock classes: MockStatsBloc (MockBloc<StatsEvent, StatsState>), MockSettingsBloc (MockBloc<SettingsEvent, SettingsState>). Fake classes: FakeStatsEvent (Fake implements StatsEvent), FakeStatsState (Fake implements StatsState), FakeSettingsEvent (Fake implements SettingsEvent), FakeSettingsState (Fake implements SettingsState).

    setUpAll: registerFallbackValue for each Fake class.

    setUp: instantiate both mocks; seed MockStatsBloc with StatsInitial (read stats_state.dart to confirm class name — it may be StatsInitial or StatsLoading); seed MockSettingsBloc with SettingsLoaded(settings: Settings.defaults()); call when().thenReturn() and whenListen() for each; register both in GetIt.instance.

    tearDown: await GetIt.instance.reset().

    Helper `_pumpStatsScreen(WidgetTester tester)`: calls pumpApp with const StatsScreen() (confirm class name from source) and providers for MockStatsBloc and MockSettingsBloc.

    Test group 'StatsScreen':
    - 'shows CircularProgressIndicator when state is StatsInitial': default seed (StatsInitial), pump, await tester.pump(), expect find.byType(CircularProgressIndicator).findsOneWidget (or find.byType(StatsLoadingView) if the screen uses a custom loading widget — read source to determine)
    - 'renders chart widgets when MonthlyStatsLoaded': override mockStatsBloc state to fakeMonthlyStatsLoaded(); pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing; verify at least one chart-related widget is present (use find.byType(CustomScrollView) or find.byType(SliverToBoxAdapter) or any widget type visible in the MonthlyStatsLoaded branch of the screen — read source to identify a reliable widget type)
    - 'shows StatsError view when StatsFailure': read stats_state.dart to confirm failure state class name (StatsError or StatsFailure); override state; pump; expect error widget (read screen to identify class name)

    Note: StatsScreen is a StatelessWidget per PATTERNS.md — no initState, no sl<> calls. GetIt registration in setUp is precautionary.

    Use single quotes, trailing commas, const constructors. Run `fvm flutter analyze test/presentation/screens/stats/stats_screen_test.dart --no-pub` and fix issues.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/presentation/screens/stats/stats_screen_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/presentation/screens/stats/stats_screen_test.dart exists
    - File contains at minimum 3 testWidgets cases (loading, loaded with stats, error state)
    - `fvm flutter test test/presentation/screens/stats/stats_screen_test.dart --no-pub` exits 0 with all tests passing
    - `fvm flutter analyze test/presentation/screens/stats/stats_screen_test.dart --no-pub` exits 0 with 0 issues
    - tearDown calls GetIt.instance.reset()
    - No CategoryBloc, IncomeBloc, ExpenseBloc, or ExportBloc providers (StatsScreen only needs StatsBloc + SettingsBloc)
  </acceptance_criteria>
  <done>stats_screen_test.dart exists with 3+ tests passing and 0 lint issues</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write settings_screen_test.dart (TEST-21)</name>
  <files>test/presentation/screens/settings/settings_screen_test.dart</files>
  <read_first>
    - lib/presentation/screens/settings/settings_screen.dart — read fully to confirm: only SettingsBloc used; PackageInfo.fromPlatform() call location (initState); exact widget types for loading/loaded states; how the biometric Switch.adaptive is wired (what event it dispatches and what key/type to find it by); what other toggle/selector widgets exist and what events they dispatch
    - lib/presentation/blocs/settings/settings_event.dart — confirm BiometricToggled(enabled: bool) constructor; confirm ThemeChanged, LocaleChanged, CurrencyChanged class names and constructors
    - lib/presentation/blocs/settings/settings_state.dart — confirm SettingsLoaded, SettingsInitial, SettingsLoading, SettingsFailure class names
    - lib/domain/entities/settings.dart — confirm Settings.defaults() factory and Settings fields (biometricEnabled, themeMode, locale, currencySymbol)
    - test/presentation/blocs/settings/settings_bloc_test.dart — read for consistency with existing test patterns for this BLoC
  </read_first>
  <action>
    Create `test/presentation/screens/settings/settings_screen_test.dart` with MockSettingsBloc pattern plus PackageInfo stub.

    Mock classes: MockSettingsBloc (MockBloc<SettingsEvent, SettingsState>). Fake classes: FakeSettingsEvent (Fake implements SettingsEvent), FakeSettingsState (Fake implements SettingsState).

    setUpAll: MUST call registerFallbackValue AND PackageInfo.setMockInitialValues BEFORE any test runs. setUpAll should contain: registerFallbackValue(FakeSettingsEvent()); registerFallbackValue(FakeSettingsState()); PackageInfo.setMockInitialValues(appName: 'Weeklet', packageName: 'com.weeklet.app', version: '1.0.0', buildNumber: '1', buildSignature: ''). The PackageInfo stub prevents MissingPluginException when SettingsScreen's initState calls PackageInfo.fromPlatform().

    setUp: mockSettingsBloc = MockSettingsBloc(); seed with SettingsLoaded(settings: Settings.defaults()) using when().thenReturn() and whenListen(); GetIt.instance.registerSingleton<SettingsBloc>(mockSettingsBloc).

    tearDown: await GetIt.instance.reset().

    Helper `_pumpSettingsScreen(WidgetTester tester)`: calls pumpApp with const SettingsScreen() and [BlocProvider<SettingsBloc>.value(value: mockSettingsBloc)].

    Test group 'SettingsScreen':
    - 'shows CircularProgressIndicator when state is SettingsInitial': override seed to SettingsInitial() (read settings_state.dart to confirm class name); pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsOneWidget
    - 'renders settings list sections when SettingsLoaded': default seed is SettingsLoaded; pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing; expect settings items are visible (find.byType(ListView) or find.byType(ListTile) — read source to identify reliable widget)
    - 'dispatches BiometricToggled when biometric switch is tapped': seed SettingsLoaded(settings: Settings.defaults()); pump; await tester.pump(); find the biometric Switch.adaptive widget (use find.byType(Switch) or find.byKey if keyed); tap it; await tester.pump(); verify(() => mockSettingsBloc.add(any(that: isA<BiometricToggled>()))).called(1). Note: read settings_screen.dart to see how the switch is structured — it may be inside a ListTile.

    Use single quotes, trailing commas, const constructors. Run `fvm flutter analyze test/presentation/screens/settings/settings_screen_test.dart --no-pub` and fix issues.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/presentation/screens/settings/settings_screen_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/presentation/screens/settings/settings_screen_test.dart exists
    - File contains at minimum 3 testWidgets cases (loading, settings list, biometric toggle dispatch)
    - `fvm flutter test test/presentation/screens/settings/settings_screen_test.dart --no-pub` exits 0 with all tests passing (no MissingPluginException)
    - `fvm flutter analyze test/presentation/screens/settings/settings_screen_test.dart --no-pub` exits 0 with 0 issues
    - setUpAll contains both registerFallbackValue calls AND PackageInfo.setMockInitialValues(appName: 'Weeklet', ...)
    - At least one test verifies BiometricToggled dispatch: verify(() => mockSettingsBloc.add(any(that: isA<BiometricToggled>()))).called(1)
    - tearDown calls GetIt.instance.reset()
  </acceptance_criteria>
  <done>settings_screen_test.dart exists with 3+ tests passing (including BiometricToggled dispatch test) and 0 lint issues; no MissingPluginException during test run</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | Plan 04 writes test files only; no production code is modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | N/A | All STRIDE categories | accept | No production code written. Test files only. PackageInfo.setMockInitialValues is a test-only stub API from the package's own test support surface. No ASVS L1 categories apply. |
</threat_model>

<verification>
After both tasks complete:
- `fvm flutter test test/presentation/screens/stats/stats_screen_test.dart --no-pub` exits 0
- `fvm flutter test test/presentation/screens/settings/settings_screen_test.dart --no-pub` exits 0 (no MissingPluginException)
- `fvm flutter test --no-pub` exits 0 (all tests passing, no regressions)
- `fvm flutter analyze test/presentation/screens/stats/ test/presentation/screens/settings/ --no-pub` exits 0
</verification>

<success_criteria>
- stats_screen_test.dart: 3+ tests covering StatsInitial loading, MonthlyStatsLoaded chart rendering, StatsError; StatsBloc + SettingsBloc only
- settings_screen_test.dart: 3+ tests covering loading, SettingsLoaded list, BiometricToggled dispatch; PackageInfo.setMockInitialValues in setUpAll
- fvm flutter test --no-pub exits 0 (no regressions)
</success_criteria>

<output>
Create `.planning/phases/12-widget-component-tests/12-04-SUMMARY.md` when done.
</output>
