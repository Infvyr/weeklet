---
phase: 12-widget-component-tests
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - test/presentation/screens/expenses/expenses_screen_test.dart
  - test/presentation/screens/expenses/add_expense_form_view_test.dart
autonomous: true
requirements: [TEST-16, TEST-17]

must_haves:
  truths:
    - "ExpensesScreen shows CircularProgressIndicator when state is ExpenseLoading"
    - "ExpensesScreen shows EmptyStateView when filteredExpenses is empty and CategoryBloc is in CategoriesLoaded"
    - "ExpensesScreen shows expense list widgets when filteredExpenses is non-empty"
    - "AddExpenseFormView shows validation errors when save is attempted with empty amount field"
    - "AddExpenseFormView dispatches AddExpenseStarted to ExpenseBloc when form is valid and submitted"
  artifacts:
    - path: "test/presentation/screens/expenses/expenses_screen_test.dart"
      provides: "Widget tests for TEST-16 (rendering + event dispatch)"
      exports: ["main"]
    - path: "test/presentation/screens/expenses/add_expense_form_view_test.dart"
      provides: "Widget tests for TEST-17 (form validation + submission)"
      exports: ["main"]
  key_links:
    - from: "test/presentation/screens/expenses/expenses_screen_test.dart"
      to: "lib/presentation/screens/expenses/expenses_screen.dart"
      via: "testWidgets + pumpApp"
      pattern: "ExpensesScreen"
    - from: "test/presentation/screens/expenses/add_expense_form_view_test.dart"
      to: "lib/presentation/screens/expenses/widgets/add_expense_form_view.dart"
      via: "testWidgets + pumpApp + GetIt registration"
      pattern: "AddExpenseFormView"
---

<objective>
Write testWidgets-based tests for ExpensesScreen (TEST-16) and AddExpenseFormView (TEST-17). These are the two most complex widget tests in the phase because ExpensesScreen requires 4 MockBloc providers and AddExpenseFormView requires GetIt registration of CategoryBloc before pumping (initState calls sl<CategoryBloc>()).

Purpose: Verify that ExpensesScreen renders the correct widget tree for each state (loading, success with items, success empty) and dispatches the correct event on interaction. Verify that AddExpenseFormView validates required fields and dispatches AddExpenseStarted on valid submit.

Output: Two test files covering TEST-16 and TEST-17.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-CONTEXT.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-RESEARCH.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-PATTERNS.md

<!-- Prior plan summary for helper imports -->
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/12-widget-component-tests/12-01-SUMMARY.md

<interfaces>
<!-- Source contracts the executor needs. Read these files before writing tests. -->

From lib/presentation/screens/expenses/expenses_screen.dart:
  - BLoC dependencies (confirmed): ExpenseBloc, CategoryBloc, SettingsBloc, ExportBloc
  - No sl<> calls in initState (AppInitializer handles initial loads)
  - Loading state → CircularProgressIndicator
  - ExpenseSuccess with filteredExpenses.isEmpty + CategoriesLoaded → EmptyStateView
  - ExpenseSuccess with filteredExpenses non-empty + CategoriesLoaded → ExpenseListView

From lib/presentation/screens/expenses/widgets/add_expense_form_view.dart:
  - BLoC dependencies: ExpenseBloc, CategoryBloc, SettingsBloc
  - initState calls: sl<CategoryBloc>().add(const GetAllCategoriesEvent())
  - GetIt MUST have CategoryBloc registered BEFORE pumpWidget or StateError is thrown
  - _onSave calls sl<AddCategoryUseCase>() only if no 'Daily' category — avoid by seeding CategoriesLoaded with a 'Daily' category
  - Valid form + save → context.read<ExpenseBloc>().add(AddExpenseStarted(...))

MockBloc pattern (per D-01, D-04):
  class MockExpenseBloc extends MockBloc<ExpenseEvent, ExpenseState> {}
  class MockCategoryBloc extends MockBloc<CategoryEvent, CategoryState> {}
  class MockSettingsBloc extends MockBloc<SettingsEvent, SettingsState> {}
  class MockExportBloc extends MockBloc<ExportEvent, ExportState> {}
  class FakeExpenseEvent extends Fake implements ExpenseEvent {}
  class FakeExpenseState extends Fake implements ExpenseState {}
  (repeat Fake classes for each BLoC type)

GetIt isolation (per D-08):
  setUp: GetIt.instance.registerSingleton<XxxBloc>(mockXxxBloc)
  tearDown: await GetIt.instance.reset()

CategoryState/Event NOT Equatable — use when(() => mock.state).thenReturn(...)
CategoryBloc initial state is CategoryLoading() (no CategoryInitial class)
ExpenseSuccess is Equatable — can use equality in expectations

State seeding with whenListen:
  whenListen(mock, const Stream<XxxState>.empty(), initialState: seedState)
  when(() => mock.state).thenReturn(seedState)
  (both calls required — whenListen sets stream, when() sets state getter)

Import paths:
  import 'package:bloc_test/bloc_test.dart';
  import 'package:flutter_bloc/flutter_bloc.dart';
  import 'package:flutter_test/flutter_test.dart';
  import 'package:get_it/get_it.dart';
  import 'package:mocktail/mocktail.dart';
  import 'package:weeklet/domain/entities/settings.dart';
  import 'package:weeklet/presentation/blocs/category/category_bloc.dart';
  import 'package:weeklet/presentation/blocs/category/category_event.dart';
  import 'package:weeklet/presentation/blocs/category/category_state.dart';
  import 'package:weeklet/presentation/blocs/expense/expense_bloc.dart';
  import 'package:weeklet/presentation/blocs/expense/expense_event.dart';
  import 'package:weeklet/presentation/blocs/expense/expense_state.dart';
  import 'package:weeklet/presentation/blocs/export/export_bloc.dart';
  import 'package:weeklet/presentation/blocs/export/export_event.dart';
  import 'package:weeklet/presentation/blocs/export/export_state.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_bloc.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_event.dart';
  import 'package:weeklet/presentation/blocs/settings/settings_state.dart';
  import 'package:weeklet/presentation/screens/expenses/expenses_screen.dart';
  import '../../../../helpers/test_app.dart';
  import '../../../../helpers/test_data.dart';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write expenses_screen_test.dart (TEST-16)</name>
  <files>test/presentation/screens/expenses/expenses_screen_test.dart</files>
  <read_first>
    - lib/presentation/screens/expenses/expenses_screen.dart — read fully to identify: all BLoC imports used, every BlocBuilder/BlocListener, what widget type is rendered for each state, any sl<> calls in initState
    - lib/presentation/blocs/expense/expense_state.dart — confirm ExpenseSuccess constructor params and copyWith signature
    - lib/presentation/blocs/export/export_state.dart — confirm ExportInitial is the correct initial state class name
    - lib/presentation/blocs/category/category_state.dart — confirm CategoriesLoaded constructor (categories field name)
    - test/presentation/blocs/expense/expense_bloc_test.dart — read setUp/tearDown pattern and _fakeExpense/_successSeed helpers for consistency
    - test/helpers/test_app.dart — confirm pumpApp signature (created by Plan 01)
    - test/helpers/test_data.dart — confirm available factory functions (created by Plan 01)
  </read_first>
  <action>
    Create `test/presentation/screens/expenses/expenses_screen_test.dart` with the following structure and test cases:

    File header: mock class declarations for MockExpenseBloc, MockCategoryBloc, MockSettingsBloc, MockExportBloc and Fake classes (FakeExpenseEvent, FakeExpenseState, FakeCategoryEvent, FakeCategoryState, FakeSettingsEvent, FakeSettingsState, FakeExportEvent, FakeExportState).

    setUpAll: registerFallbackValue for one instance of each Fake class.

    setUp: instantiate all 4 mocks; call when(() => mock.state).thenReturn(seedState) and whenListen(mock, const Stream.empty(), initialState: seedState) for each; register all 4 in GetIt.instance as singletons. Default seed states: ExpenseLoading(), CategoryLoading(), SettingsLoaded(settings: Settings.defaults()), ExportInitial().

    tearDown: await GetIt.instance.reset().

    Helper function `_pumpExpensesScreen(WidgetTester tester)` that calls pumpApp with const ExpensesScreen() and all 4 BlocProvider.value providers.

    Test group 'ExpensesScreen':
    - 'shows CircularProgressIndicator when state is ExpenseLoading': seed ExpenseLoading, pump, await tester.pump(), expect find.byType(CircularProgressIndicator).findsOneWidget
    - 'shows EmptyStateView when filteredExpenses is empty': override mockExpenseBloc state to fakeExpenseSuccess(expenses: []) and mockCategoryBloc state to CategoriesLoaded(categories: [fakeCategory()]); pump; await tester.pump(); expect find.byType(EmptyStateView).findsOneWidget (use the actual EmptyStateView widget class name — read the screen file to confirm the class name)
    - 'shows expense list when filteredExpenses is non-empty': override mockExpenseBloc state to fakeExpenseSuccess(expenses: [fakeExpense()]); override mockCategoryBloc state to CategoriesLoaded(categories: [fakeCategory()]); pump; await tester.pump(); expect find.byType(CircularProgressIndicator).findsNothing
    - 'renders filter area without asserting dispatch' (scope: rendering only): after pumping with the non-empty ExpenseSuccess seed, assert that the year-selector widget is present in the tree (find.byType or find.text). Do NOT tap or verify event dispatch in this test — event dispatch for this screen is fully covered by Task 2's 'dispatches AddExpenseStarted when valid form is submitted' test, satisfying D-02 for the phase. Add a comment in the test body: // D-02 event-dispatch coverage for ExpensesScreen is provided by add_expense_form_view_test.dart Task 2 (AddExpenseStarted verify).

    D-02 compliance note: Task 1 tests are scoped to rendering verification only. The mandatory event-dispatch verification required by D-02 is delivered by Task 2 (add_expense_form_view_test.dart), which uses verify(() => mockExpenseBloc.add(any(that: isA<AddExpenseStarted>()))).called(1) as a concrete, passing assertion.

    Use single quotes, trailing commas, const constructors. Import path from test file to helpers: `../../../../helpers/test_app.dart` and `../../../../helpers/test_data.dart`. Run `fvm flutter analyze test/presentation/screens/expenses/expenses_screen_test.dart --no-pub` and fix any lint issues before committing.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/presentation/screens/expenses/expenses_screen_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/presentation/screens/expenses/expenses_screen_test.dart exists
    - File contains at minimum 3 testWidgets cases (loading state, empty state, list state)
    - `fvm flutter test test/presentation/screens/expenses/expenses_screen_test.dart --no-pub` exits 0 with all tests passing
    - `fvm flutter analyze test/presentation/screens/expenses/expenses_screen_test.dart --no-pub` exits 0 with 0 issues
    - setUpAll contains registerFallbackValue calls for FakeExpenseEvent, FakeExpenseState, FakeCategoryEvent, FakeCategoryState, FakeSettingsEvent, FakeSettingsState, FakeExportEvent, FakeExportState
    - tearDown calls GetIt.instance.reset()
  </acceptance_criteria>
  <done>expenses_screen_test.dart exists with all tests passing and 0 lint issues</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write add_expense_form_view_test.dart (TEST-17)</name>
  <files>test/presentation/screens/expenses/add_expense_form_view_test.dart</files>
  <read_first>
    - lib/presentation/screens/expenses/widgets/add_expense_form_view.dart — read fully to identify: initState sl<> calls, form structure, validation triggers, AddExpenseStarted dispatch path, how _onSave uses CategoriesLoaded state
    - lib/presentation/blocs/expense/expense_event.dart — confirm AddExpenseStarted constructor params (needed for verify() call)
    - lib/presentation/blocs/category/category_state.dart — confirm CategoriesLoaded constructor (needed to seed with a 'Daily' category to bypass sl<AddCategoryUseCase> path)
    - lib/domain/entities/category.dart — confirm Category constructor to create a 'Daily' category in test
    - test/helpers/test_app.dart — confirm pumpApp signature
    - test/helpers/test_data.dart — confirm fakeCategory factory
  </read_first>
  <action>
    Create `test/presentation/screens/expenses/add_expense_form_view_test.dart` with the following structure:

    Mock class declarations: MockExpenseBloc, MockCategoryBloc, MockSettingsBloc (no ExportBloc needed — add_expense_form_view.dart does not use ExportBloc). Fake classes: FakeExpenseEvent, FakeExpenseState, FakeCategoryEvent, FakeCategoryState, FakeSettingsEvent, FakeSettingsState.

    setUpAll: registerFallbackValue for each Fake class.

    setUp: instantiate mocks; seed: ExpenseLoading() for expense, CategoryLoading() for category (then override per-test to CategoriesLoaded), SettingsLoaded(settings: Settings.defaults()) for settings; register all in GetIt.instance. CRITICAL: GetIt registration of CategoryBloc MUST happen in setUp before any pumpWidget call because add_expense_form_view.dart calls sl<CategoryBloc>().add(const GetAllCategoriesEvent()) in initState.

    tearDown: await GetIt.instance.reset().

    Helper `_pumpFormView(WidgetTester tester)` that overrides mockCategoryBloc state to CategoriesLoaded with a 'Daily' category: `CategoriesLoaded(categories: [fakeCategory(name: 'Daily')])` then calls pumpApp with AddExpenseFormView() and [BlocProvider.value(value: mockExpenseBloc), BlocProvider.value(value: mockCategoryBloc), BlocProvider.value(value: mockSettingsBloc)].

    Test group 'AddExpenseFormView':
    - 'shows form fields when categories are loaded': call _pumpFormView; await tester.pump(); expect find.byType(TextFormField).findsWidgets (at least 1 field visible)
    - 'shows validation error for empty amount on submit attempt': call _pumpFormView; find the submit/save button (read add_expense_form_view.dart to identify button type/text/key); tap it; await tester.pump(); expect find.text that matches the required validation string (read localization key for empty/required from the validator). Note: the form may disable the submit button if _hasInteracted is false — in that case, enter a description text first to trigger _hasInteracted, then clear amount and tap submit.
    - 'dispatches AddExpenseStarted when valid form is submitted': call _pumpFormView; enter a valid amount in the amount field (e.g., '50'); enter description text; tap submit; await tester.pump(); verify(() => mockExpenseBloc.add(any(that: isA<AddExpenseStarted>()))).called(1) — this verifies D-02 (event dispatch verification on interaction)

    For the submit button: read the actual widget type from add_expense_form_view.dart (it may be ElevatedButton, TextButton, or a custom button widget). Use find.byType(ElevatedButton) or find.text(submitText) based on what the file shows.

    Use single quotes, trailing commas, const constructors. Run `fvm flutter analyze test/presentation/screens/expenses/add_expense_form_view_test.dart --no-pub` and fix issues.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter test test/presentation/screens/expenses/add_expense_form_view_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/presentation/screens/expenses/add_expense_form_view_test.dart exists
    - File contains at minimum 3 testWidgets cases
    - `fvm flutter test test/presentation/screens/expenses/add_expense_form_view_test.dart --no-pub` exits 0 with all tests passing
    - `fvm flutter analyze test/presentation/screens/expenses/add_expense_form_view_test.dart --no-pub` exits 0 with 0 issues
    - setUp registers CategoryBloc in GetIt BEFORE pumpWidget is called (required because initState calls sl<CategoryBloc>())
    - At least one test verifies event dispatch: verify(() => mockExpenseBloc.add(any(that: isA<AddExpenseStarted>()))).called(1)
  </acceptance_criteria>
  <done>add_expense_form_view_test.dart exists with all tests passing and 0 lint issues; AddExpenseStarted dispatch is verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code only | Plan 02 writes test files only; no production code is modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | N/A | All STRIDE categories | accept | No production code written. Test files test existing screens. No auth, session, data, or network paths modified. No ASVS L1 categories apply. |
</threat_model>

<verification>
After both tasks complete:
- `fvm flutter test test/presentation/screens/expenses/ --no-pub` exits 0 (both test files pass)
- `fvm flutter test --no-pub` exits 0 (130+ tests passing, no regressions)
- `fvm flutter analyze test/presentation/screens/expenses/ --no-pub` exits 0
- Each test file has setUpAll registerFallbackValue calls, setUp GetIt registration, tearDown GetIt.instance.reset()
</verification>

<success_criteria>
- expenses_screen_test.dart: 3+ tests covering loading, empty state, list state for ExpensesScreen
- add_expense_form_view_test.dart: 3+ tests covering form rendering, validation error, AddExpenseStarted dispatch
- fvm flutter test test/presentation/screens/expenses/ --no-pub exits 0
- fvm flutter test --no-pub exits 0 (no regressions)
</success_criteria>

<output>
Create `.planning/phases/12-widget-component-tests/12-02-SUMMARY.md` when done.
</output>
