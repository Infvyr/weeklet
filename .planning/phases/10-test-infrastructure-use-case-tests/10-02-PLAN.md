---
plan: "10-02"
phase: 10
wave: 1
depends_on: []
title: "TEST-04 — Delete use case tests (expense, income, category)"
objective: "Create unit test files for DeleteExpenseUseCase, DeleteIncomeUseCase, and DeleteCategoryUseCase covering validation and delegation behaviors."
requirements: [TEST-04]
files_modified:
  - test/domain/usecases/expense/delete_expense_usecase_test.dart
  - test/domain/usecases/income/delete_income_use_case_test.dart
  - test/domain/usecases/category/delete_category_usecase_test.dart
autonomous: true
---

<objective>
Create three delete use case test files. Each tests what the production code actually does:
- DeleteExpenseUseCase: validates empty ID → throws; valid ID → delegates (no throw)
- DeleteIncomeUseCase: NO validation at all — only tests delegation (one test)
- DeleteCategoryUseCase: validates empty/whitespace ID → throws; valid ID → delegates (no throw)

Purpose: TEST-04 requires delete use case coverage. Production implementations differ
importantly: DeleteExpenseUseCase validates `params.isEmpty`, DeleteCategoryUseCase validates
`params.trim().isEmpty`, but DeleteIncomeUseCase has zero validation and just calls
`repository.deleteIncome(id)` directly. Tests must reflect actual behavior, not aspirational.

Output:
- test/domain/usecases/expense/delete_expense_usecase_test.dart
- test/domain/usecases/income/delete_income_use_case_test.dart
- test/domain/usecases/category/delete_category_usecase_test.dart
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/ROADMAP.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.planning/phases/10-test-infrastructure-use-case-tests/10-CONTEXT.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From lib/domain/usecases/expense/delete_expense_usecase.dart:
```dart
class DeleteExpenseUseCase implements UseCase<void, String> {
  const DeleteExpenseUseCase(this.repository);
  final ExpenseRepository repository;

  Future<void> call(String params) async {
    if (params.isEmpty) {
      throw const ExpenseValidationException(ExpenseValidationError.emptyExpenseId);
    }
    await repository.deleteExpense(params);
  }
}
```

From lib/domain/usecases/income/delete_income_use_case.dart:
```dart
class DeleteIncomeUseCase {
  const DeleteIncomeUseCase(this.repository);
  final IncomeRepository repository;

  Future<void> call(String id) => repository.deleteIncome(id);
  // NO validation — no empty check, no exception throwing
}
```

From lib/domain/usecases/category/delete_category_usecase.dart:
```dart
class DeleteCategoryUseCase implements UseCase<void, String> {
  DeleteCategoryUseCase(this.repository);
  final CategoryRepository repository;

  Future<void> call(String params) async {
    if (params.trim().isEmpty) {
      throw const CategoryValidationException(CategoryValidationError.emptyCategoryId);
    }
    return repository.deleteCategory(params);
  }
}
```

From lib/domain/exceptions/expense_exceptions.dart:
```dart
enum ExpenseValidationError { invalidAmount, emptyDescription, emptyCategory, emptyExpenseId }
class ExpenseValidationException implements Exception {
  const ExpenseValidationException(this.error);
  final ExpenseValidationError error;
}
```

From lib/domain/exceptions/category_exceptions.dart:
```dart
enum CategoryValidationError { emptyName, emptyIcon, emptyId, emptyCategoryId, duplicateName }
// Note: duplicateName is added by plan 10-01, but since plans run in parallel (wave 1),
// this plan must NOT depend on it. The delete tests only use emptyCategoryId.
class CategoryValidationException implements Exception {
  const CategoryValidationException(this.error);
  final CategoryValidationError error;
}
```

Repository interfaces (for stubs — implement all methods):
- ExpenseRepository: addExpense, deleteExpense, updateExpense, getExpensesByMonthYear, getAllExpenses, clearAll
- IncomeRepository: addIncome, getIncomes, updateIncome, deleteIncome, clearAll
- CategoryRepository: addCategory, deleteCategory, updateCategory, getAllCategories, getCategoryById, clearAll

File naming conventions (MUST match production file suffix):
- Expense use case file ends in `usecase.dart` → test file: `delete_expense_usecase_test.dart`
- Income use case file ends in `use_case.dart` (two words) → test file: `delete_income_use_case_test.dart`
- Category use case file ends in `usecase.dart` → test file: `delete_category_usecase_test.dart`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DeleteExpenseUseCase test</name>
  <files>test/domain/usecases/expense/delete_expense_usecase_test.dart</files>

  <read_first>
    - lib/domain/usecases/expense/delete_expense_usecase.dart (production code to test)
    - lib/domain/exceptions/expense_exceptions.dart (ExpenseValidationError.emptyExpenseId)
    - lib/domain/repositories/expense_repository.dart (all methods to stub)
    - test/domain/usecases/expense/add_expense_usecase_test.dart (canonical pattern to follow exactly)
  </read_first>

  <action>
    Create test/domain/usecases/expense/delete_expense_usecase_test.dart.

    Imports:
    - `package:flutter_test/flutter_test.dart`
    - `package:weeklet/domain/exceptions/expense_exceptions.dart`
    - `package:weeklet/domain/repositories/expense_repository.dart`
    - `package:weeklet/domain/usecases/expense/delete_expense_usecase.dart`
    - `package:weeklet/domain/entities/expense.dart`

    Stub class `_StubExpenseRepository implements ExpenseRepository`:
    - `String? lastDeletedId;` field
    - `deleteExpense(String id)`: sets `lastDeletedId = id`, returns `Future<void>`
    - All other methods: minimal no-op implementations returning appropriate types
      (getAllExpenses → [], getExpensesByMonthYear → [], addExpense → async {}, updateExpense → async {}, clearAll → async {})

    SetUp: `repository = _StubExpenseRepository(); useCase = const DeleteExpenseUseCase(repository);`
    Note: `DeleteExpenseUseCase` constructor is `const`.

    Test group `'DeleteExpenseUseCase — validation'`:
    - `'throws ExpenseValidationException(emptyExpenseId) for empty id'`:
      `expect(() async => useCase(''), throwsA(isA<ExpenseValidationException>().having((e) => e.error, 'error', ExpenseValidationError.emptyExpenseId)))`
    - `'delegates to repository for valid id without throwing'`:
      `await expectLater(useCase('expense-123'), completes);`
      Then assert `repository.lastDeletedId == 'expense-123'`

    Linting: single quotes, trailing commas on all multi-line argument lists, `const`
    constructors where applicable, `final` for all stub fields.

    Run: `fvm flutter test test/domain/usecases/expense/delete_expense_usecase_test.dart --no-pub`
  </action>

  <verify>
    <automated>fvm flutter test test/domain/usecases/expense/delete_expense_usecase_test.dart --no-pub</automated>
  </verify>

  <acceptance_criteria>
    - File exists at test/domain/usecases/expense/delete_expense_usecase_test.dart
    - Contains exactly one group: `'DeleteExpenseUseCase — validation'`
    - Test 1: calling `useCase('')` throws `ExpenseValidationException` with `.error == ExpenseValidationError.emptyExpenseId`
    - Test 2: calling `useCase('expense-123')` completes without throwing AND `repository.lastDeletedId` equals `'expense-123'`
    - `fvm flutter test test/domain/usecases/expense/delete_expense_usecase_test.dart --no-pub` outputs `All tests passed!`
  </acceptance_criteria>

  <done>
    File exists with 2 tests, all GREEN, matching the canonical stub+group pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: DeleteIncomeUseCase and DeleteCategoryUseCase tests</name>
  <files>
    test/domain/usecases/income/delete_income_use_case_test.dart
    test/domain/usecases/category/delete_category_usecase_test.dart
  </files>

  <read_first>
    - lib/domain/usecases/income/delete_income_use_case.dart (no validation — stub delegation only)
    - lib/domain/usecases/category/delete_category_usecase.dart (trim().isEmpty check, emptyCategoryId)
    - lib/domain/exceptions/category_exceptions.dart (CategoryValidationError.emptyCategoryId)
    - lib/domain/repositories/income_repository.dart (all methods to stub)
    - lib/domain/repositories/category_repository.dart (all methods to stub)
    - test/domain/usecases/income/add_income_use_case_test.dart (income file naming and pattern)
  </read_first>

  <action>
    Create test/domain/usecases/income/delete_income_use_case_test.dart.

    IMPORTANT: DeleteIncomeUseCase has NO validation. Its call() does:
    `Future<void> call(String id) => repository.deleteIncome(id);`
    There is no empty-ID check. Do NOT write a test asserting it throws for empty input — that
    test would fail (no exception is thrown). Write only what the production code actually does.

    Imports for income test file:
    - `package:flutter_test/flutter_test.dart`
    - `package:weeklet/domain/repositories/income_repository.dart`
    - `package:weeklet/domain/usecases/income/delete_income_use_case.dart`
    - `package:weeklet/domain/entities/income.dart`

    Income stub `_StubIncomeRepository implements IncomeRepository`:
    - `String? lastDeletedId;`
    - `deleteIncome(String id)`: sets `lastDeletedId = id`, returns `Future<void>`
    - `addIncome`, `updateIncome`, `clearAll`: async no-ops
    - `getIncomes`: returns `[]`

    SetUp: `repository = _StubIncomeRepository(); useCase = const DeleteIncomeUseCase(repository);`

    Test group `'DeleteIncomeUseCase — delegation'`:
    - `'delegates to repository without throwing for any id'`:
      `await expectLater(useCase('income-456'), completes);`
      Assert `repository.lastDeletedId == 'income-456'`

    ---

    Create test/domain/usecases/category/delete_category_usecase_test.dart.

    Imports:
    - `package:flutter_test/flutter_test.dart`
    - `package:weeklet/domain/entities/category.dart`
    - `package:weeklet/domain/exceptions/category_exceptions.dart`
    - `package:weeklet/domain/repositories/category_repository.dart`
    - `package:weeklet/domain/usecases/category/delete_category_usecase.dart`

    Category stub `_StubCategoryRepository implements CategoryRepository`:
    - `String? lastDeletedId;`
    - `deleteCategory(String id)`: sets `lastDeletedId = id`, returns `Future<void>`
    - `addCategory`, `updateCategory`, `clearAll`: async no-ops
    - `getAllCategories`: returns `[]`
    - `getCategoryById`: returns `Future.value(null)`

    SetUp: `repository = _StubCategoryRepository(); useCase = DeleteCategoryUseCase(repository);`
    Note: `DeleteCategoryUseCase` constructor is NOT const (no `const` keyword in source).

    Test group `'DeleteCategoryUseCase — validation'`:
    - `'throws CategoryValidationException(emptyCategoryId) for empty id'`:
      `expect(() async => useCase(''), throwsA(isA<CategoryValidationException>().having((e) => e.error, 'error', CategoryValidationError.emptyCategoryId)))`
    - `'throws CategoryValidationException(emptyCategoryId) for whitespace-only id'`:
      `expect(() async => useCase('   '), throwsA(isA<CategoryValidationException>().having((e) => e.error, 'error', CategoryValidationError.emptyCategoryId)))`
    - `'delegates to repository for valid id without throwing'`:
      `await expectLater(useCase('cat-789'), completes);`
      Assert `repository.lastDeletedId == 'cat-789'`

    Apply linting rules to both files: single quotes, trailing commas, const where applicable, final fields.

    Run both:
    `fvm flutter test test/domain/usecases/income/delete_income_use_case_test.dart --no-pub`
    `fvm flutter test test/domain/usecases/category/delete_category_usecase_test.dart --no-pub`
    Then run full suite: `fvm flutter test test/ --no-pub`
  </action>

  <verify>
    <automated>fvm flutter test test/domain/usecases/income/delete_income_use_case_test.dart --no-pub</automated>
    <automated>fvm flutter test test/domain/usecases/category/delete_category_usecase_test.dart --no-pub</automated>
    <automated>fvm flutter test test/ --no-pub</automated>
  </verify>

  <acceptance_criteria>
    Income test file:
    - Exists at test/domain/usecases/income/delete_income_use_case_test.dart
    - Contains group `'DeleteIncomeUseCase — delegation'` with exactly 1 test
    - The test calls `useCase('income-456')`, expects `completes`, and asserts `lastDeletedId == 'income-456'`
    - Does NOT assert any exception throwing (DeleteIncomeUseCase has no validation)

    Category test file:
    - Exists at test/domain/usecases/category/delete_category_usecase_test.dart
    - Contains group `'DeleteCategoryUseCase — validation'` with 3 tests
    - Empty string `''` throws `CategoryValidationException` with `.error == CategoryValidationError.emptyCategoryId`
    - Whitespace `'   '` throws `CategoryValidationException` with `.error == CategoryValidationError.emptyCategoryId`
    - Valid id `'cat-789'` completes without throwing AND sets `lastDeletedId == 'cat-789'`

    Full suite: `fvm flutter test test/ --no-pub` outputs `All tests passed!`
  </acceptance_criteria>

  <done>
    Both files exist, all tests GREEN, full suite passes with zero failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan creates test-only files. No production code is modified. No new packages installed.

| Boundary | Description |
|----------|-------------|
| test code → production use cases | Tests call domain use cases via manual stubs; no real Hive or DI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | Test files | accept | Test files contain no secrets or PII — only stub data like 'expense-123' |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |

No new attack surface introduced. Testing-only plan.
</threat_model>

<must_haves>
  <truths>
    - DeleteExpenseUseCase test: empty string ID causes ExpenseValidationException(emptyExpenseId)
    - DeleteExpenseUseCase test: valid ID delegates to repository (no throw, lastDeletedId is set)
    - DeleteIncomeUseCase test: any ID delegates to repository (one test, no exception assertions)
    - DeleteCategoryUseCase test: empty or whitespace ID causes CategoryValidationException(emptyCategoryId)
    - DeleteCategoryUseCase test: valid ID delegates to repository (no throw, lastDeletedId is set)
    - Full suite passes with zero failures after both new files are created
  </truths>
  <artifacts>
    - path: "test/domain/usecases/expense/delete_expense_usecase_test.dart"
      provides: "DeleteExpenseUseCase validation + delegation tests"
      contains: "emptyExpenseId"
    - path: "test/domain/usecases/income/delete_income_use_case_test.dart"
      provides: "DeleteIncomeUseCase delegation test"
      contains: "DeleteIncomeUseCase"
    - path: "test/domain/usecases/category/delete_category_usecase_test.dart"
      provides: "DeleteCategoryUseCase validation + delegation tests"
      contains: "emptyCategoryId"
  </artifacts>
  <key_links>
    - from: "delete_expense_usecase_test.dart"
      to: "ExpenseValidationError.emptyExpenseId"
      via: "throwsA(isA<ExpenseValidationException>().having(...))"
      pattern: "emptyExpenseId"
    - from: "delete_category_usecase_test.dart"
      to: "CategoryValidationError.emptyCategoryId"
      via: "throwsA(isA<CategoryValidationException>().having(...))"
      pattern: "emptyCategoryId"
  </key_links>
</must_haves>

<verification>
Run after all files are created:

```
fvm flutter test test/ --no-pub
```

Expected output: `All tests passed!` with zero failures.
</verification>

<success_criteria>
- 3 new test files exist (delete_expense_usecase_test.dart, delete_income_use_case_test.dart, delete_category_usecase_test.dart)
- DeleteIncomeUseCase test has exactly 1 test (delegation only, no exception test — matches production code reality)
- DeleteCategoryUseCase test has 3 tests (empty, whitespace, valid)
- DeleteExpenseUseCase test has 2 tests (empty, valid)
- Full suite `fvm flutter test test/ --no-pub` passes with zero failures
</success_criteria>

<output>
Create `.planning/phases/10-test-infrastructure-use-case-tests/10-02-SUMMARY.md` when done.
</output>
