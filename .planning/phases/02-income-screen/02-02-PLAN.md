---
phase: 02-income-screen
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - INC-01
  - INC-02
  - INC-03
  - INC-04

must_haves:
  truths:
    - "Income list renders entries grouped by week within the selected month"
    - "App bar shows 'My Income' and total card shows English labels"
    - "Adding an income entry persists it and updates statistics screen totals"
    - "Editing an income entry reflects the change in the list and statistics screen"
    - "Deleting an income entry removes it from the list and updates statistics screen totals"
  artifacts:
    - path: "lib/presentation/screens/income/income_screen.dart"
      provides: "Navigable income screen"
      contains: "My Income"
    - path: "lib/presentation/blocs/income/income_bloc.dart"
      provides: "CRUD with stats sync"
      contains: "sl<StatsBloc>().add(LoadMonthlyStats"
  key_links:
    - from: "IncomeScreen"
      to: "StatsScreen"
      via: "sl<StatsBloc>().add(LoadMonthlyStats) after CRUD"
      pattern: "manually verify stats totals update"
---

<objective>
Human verification of all four INC requirements after Plan 01 code changes are in place.
Confirms weekly grouping renders correctly, English strings are visible, CRUD operations
work without stale data, and statistics screen totals reflect income mutations.

Purpose: No automated test infrastructure exists in this project (STATE.md). All four
requirements are UI/integration behaviors that must be verified by direct interaction with
the running app on a device or simulator.

Output: User confirms or reports issues for all four requirements.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-income-screen/02-01-SUMMARY.md
@.planning/phases/02-income-screen/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all four INC requirements in running app</name>
  <files>none — verification only, no code changes</files>
  <read_first>
    - .planning/phases/02-income-screen/02-01-SUMMARY.md (confirm Plan 01 completed cleanly)
  </read_first>
  <action>
    No code changes in this plan. This is a manual verification checkpoint.

    Before presenting to the user, read 02-01-SUMMARY.md and confirm Plan 01 completed
    successfully (all tasks committed, zero analyzer errors). If Plan 01 is incomplete,
    do not proceed — return to Plan 01 execution.

    Then present the verification steps below to the user and wait for their response.
  </action>
  <what-built>
    Plan 01 made three targeted changes:
    1. income_screen.dart app bar title: 'Venituri' → 'My Income'
    2. income_total_card.dart label: Romanian → 'Total income for {month}' / 'Total income'
    3. income_bloc.dart: added sl&lt;StatsBloc&gt;().add(LoadMonthlyStats(year: DateTime.now().year))
       to _onAddIncome, _onUpdateIncome, and _onDeleteIncome (after add(const LoadIncomesRequested()))
  </what-built>
  <how-to-verify>
    Run the app first:
    ```bash
    fvm flutter run
    ```
    Navigate to the Income screen (bottom nav or menu). Then verify each requirement:

    **INC-02 — English strings (visual)**
    - App bar title must read: `My Income` (not 'Venituri')
    - Total summary card label must read: `Total income for {month}` or `Total income`
    - Empty state (if no entries) must read: `No income yet` with subtitle `Tap + to add your first income`
    - No Romanian text visible anywhere on the income screen

    **INC-01 — Weekly grouping**
    - With at least 2 income entries in the same month on different dates:
      entries must appear under week headers (e.g., "Week 14", "Week 15")
    - Each week section must show a week subtotal row
    - Changing the month filter must update the displayed groups

    **INC-04 — Add / Edit / Delete correctness**
    - Tap FAB → bottom sheet opens with header `Add Income`
    - Fill in amount + description → tap `Add Income` → sheet closes automatically
    - New entry appears in the list immediately (no stale data)
    - Long-press or tap the menu (⋮) on an entry → tap Edit → sheet opens pre-filled
    - Change the amount → tap `Update Income` → sheet closes → list shows updated value
    - Long-press or tap the menu on an entry → tap Delete → confirmation dialog appears
    - Confirm delete → entry disappears from list

    **INC-03 — Stats sync after CRUD**
    - After adding an income entry: navigate to Statistics screen
    - Statistics screen total income for the current month must include the newly added entry's amount
    - Navigate back, delete the entry → return to Statistics → total must decrease accordingly
    - (If Statistics screen is not showing current month data, use the year/month filter to select
      the current month)
  </how-to-verify>
  <verify>User response is "approved" or a description of issues found.</verify>
  <resume-signal>
    Type "approved" if all four requirements pass.
    Or describe any issues found (e.g., "INC-03 stats not updating — income total unchanged after add").
  </resume-signal>
  <acceptance_criteria>
    - App bar shows 'My Income' — visible in running app
    - Total card label contains 'Total income' — visible in running app
    - Income entries group by week under named week headers — visible in running app
    - Adding an income entry: sheet closes automatically, entry appears in list
    - Editing an income entry: updated amount appears in list after sheet closes
    - Deleting an income entry: entry disappears from list after confirmation
    - Statistics screen income total changes after adding/deleting income entries
  </acceptance_criteria>
  <done>User confirms all four requirements (INC-01, INC-02, INC-03, INC-04) pass in the running app. Phase 2 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — verification only | No code changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | N/A | Verification plan | accept | No new code introduced; trust boundary analysis not applicable |
</threat_model>

<verification>
All verification is performed by the human during the checkpoint above.
The automated pre-condition (Plan 01 complete) is checked via SUMMARY.md read.
</verification>

<success_criteria>
User types "approved" after verifying:
1. INC-01: Weekly grouping renders correctly in income list
2. INC-02: All visible strings are English (no Romanian text)
3. INC-03: Statistics screen totals update after income add/edit/delete
4. INC-04: Add, edit, and delete operations complete without stale data
</success_criteria>

<output>
After completion, create `.planning/phases/02-income-screen/02-02-SUMMARY.md` using the
template at `.claude/get-shit-done/templates/summary.md`.
</output>
