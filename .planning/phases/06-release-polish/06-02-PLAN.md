---
phase: 06-release-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01-PLAN.md"
files_modified:
  - lib/main.dart
autonomous: false
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Weeklet icon is visible on iOS simulator home screen and is NOT the default Flutter blue logo"
    - "Weeklet icon is visible on Android emulator home screen with #1447E6 brand-blue adaptive background"
    - "Cold-launch on iOS shows a brand-blue (#1447E6) splash before the first Flutter frame"
    - "Cold-launch on Android 12+ shows a brand-blue (#1447E6) splash, not a white screen"
    - "No white flash is visible between native splash and first Flutter frame (or FlutterNativeSplash.preserve/remove added if flash is observed)"
    - "fvm flutter test continues to pass after any Dart changes made in this plan"
  artifacts:
    - path: "ios/Runner/Assets.xcassets/AppIcon.appiconset/"
      provides: "All iOS icon sizes (verified visually on simulator)"
    - path: "android/app/src/main/res/mipmap-hdpi/ic_launcher.png"
      provides: "Android launcher icon (verified visually on emulator)"
    - path: "lib/main.dart"
      provides: "Optional: FlutterNativeSplash.preserve/remove if white flash observed"
  key_links:
    - from: "Generated splash assets"
      to: "iOS Simulator cold launch"
      via: "fvm flutter run --release"
      pattern: "brand-blue splash visible"
    - from: "Generated icon assets"
      to: "Android emulator home screen"
      via: "fvm flutter run --release"
      pattern: "no default Flutter icon"
---

<objective>
Perform visual verification of generated launch assets on iOS simulator and Android
emulator, and conditionally add FlutterNativeSplash.preserve/remove to main.dart
if a white flash is observed between the native splash and first Flutter frame.

Purpose: Confirm REL-01 and REL-02 are met on real platform renderers — icon and
splash correctness can only be verified visually, not by automated test.
Output: Human sign-off that icon and splash are correct, plus an optional Dart
change to main.dart/app_initializer.dart if white flash mitigation is needed.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-release-polish/06-RESEARCH.md
@.planning/phases/06-release-polish/06-01-SUMMARY.md

<interfaces>
<!-- Key patterns for the conditional white-flash task. Read these before deciding. -->

From lib/main.dart (current state after Plan 01):
```dart
Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  // ... LocaleManager init ...
  await di.init();
  runApp(const WeekletApp());
}
```

If white flash mitigation is needed, main.dart becomes:
```dart
import 'package:flutter_native_splash/flutter_native_splash.dart';

Future<void> main() async {
  final widgetsBinding = WidgetsFlutterBinding.ensureInitialized();
  FlutterNativeSplash.preserve(widgetsBinding: widgetsBinding);
  // ... LocaleManager init ...
  await di.init();
  runApp(const WeekletApp());
}
```

And lib/presentation/app_initializer.dart — add at end of initState() after all
BLoC initial events are dispatched:
```dart
FlutterNativeSplash.remove();
```

From lib/core/theme/colors.dart:
- Brand blue: #1447E6 (lightPrimaryColor)
- Dark background: #101828 (darkBackground)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify launcher icon on iOS simulator and Android emulator</name>
  <files>ios/Runner/Assets.xcassets/AppIcon.appiconset/ android/app/src/main/res/mipmap-hdpi/ic_launcher.png</files>
  <action>
    Run the app in release mode on iOS simulator and Android emulator, navigate to the
    home screen on each platform, and confirm the Weeklet icon matches the brand design
    rather than the default Flutter blue/teal logo.

    iOS Simulator steps:
    1. Get booted simulator ID: xcrun simctl list devices booted
    2. Run: fvm flutter run --release -d <ios-simulator-id>
    3. Press Cmd+Shift+H to go to the home screen
    4. Find the Weeklet app icon — must NOT be the default Flutter blue logo

    Android Emulator steps (if available; API 26+ required for adaptive icons):
    1. Run: fvm flutter run --release -d <android-emulator-id>
    2. Long-press home screen, add Weeklet shortcut
    3. Confirm icon shows brand-blue (#1447E6) adaptive background

    iOS verification alone is sufficient to close REL-01 if no Android emulator is available.
  </action>
  <verify>
    <automated>ls "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/ios/Runner/Assets.xcassets/AppIcon.appiconset/" | wc -l</automated>
  </verify>
  <done>iOS home screen shows Weeklet icon that is NOT the default Flutter blue logo; Android emulator (if tested) shows #1447E6 adaptive background</done>
  <resume-signal>Type "icon verified" if both/iOS icon looks correct, or describe what you see if incorrect</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify native splash screen on cold launch</name>
  <files>ios/Runner/Base.lproj/LaunchScreen.storyboard android/app/src/main/res/drawable/launch_background.xml</files>
  <action>
    Perform a cold launch on iOS simulator and Android 12+ emulator to confirm the
    brand-blue splash appears before the first Flutter frame, and check for any white flash.

    iOS cold launch test:
    1. Terminate app: xcrun simctl terminate booted &lt;bundle-id&gt; or force-close from app switcher
    2. Launch fresh from the home screen icon
    3. PASS: Brand-blue (#1447E6) fills the screen during startup
    4. FAIL: White/blank screen — re-run fvm dart run flutter_native_splash:create

    White flash check:
    5. Watch transition from blue native splash to first Flutter frame
    6. PASS: Seamless blue-to-app transition (no flash)
    7. FAIL: White flash visible — note it; Task 3 will add FlutterNativeSplash.preserve/remove

    Android 12+ cold launch test (if emulator available — API 31+):
    1. Force-stop: adb shell am force-stop &lt;package-name&gt;
    2. Launch fresh from launcher
    3. PASS: Brand-blue splash (confirms android_12: block applied)
    4. FAIL: White screen with small icon — android_12: block config issue
  </action>
  <verify>
    <automated>grep -r '1447E6' "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/android/app/src/main/res/drawable/" 2>/dev/null | wc -l</automated>
  </verify>
  <done>iOS cold launch shows blue (#1447E6) splash before first Flutter frame; Android 12+ shows blue splash (not white) if emulator available; white flash presence noted for Task 3</done>
  <resume-signal>Type "splash verified — no flash", "splash verified — white flash observed", or describe what you see</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add FlutterNativeSplash.preserve/remove if white flash observed (conditional)</name>
  <read_first>
    - lib/main.dart (read current state before editing)
    - lib/presentation/app_initializer.dart (read current initState to find correct insertion point)
  </read_first>
  <files>
    lib/main.dart
    lib/presentation/app_initializer.dart
  </files>
  <action>
    **ONLY execute this task if Task 2 reported "white flash observed". If no flash was
    observed, skip this task and mark it done with "not needed — no white flash".**

    If white flash was observed:

    EDIT 1 — lib/main.dart:
    Add import at the top (after existing imports):
    ```dart
    import 'package:flutter_native_splash/flutter_native_splash.dart';
    ```

    Change the first line of main() from:
    ```dart
    WidgetsFlutterBinding.ensureInitialized();
    ```
    to:
    ```dart
    final widgetsBinding = WidgetsFlutterBinding.ensureInitialized();
    FlutterNativeSplash.preserve(widgetsBinding: widgetsBinding);
    ```

    All subsequent lines in main() remain unchanged.

    EDIT 2 — lib/presentation/app_initializer.dart:
    Add import at the top:
    ```dart
    import 'package:flutter_native_splash/flutter_native_splash.dart';
    ```

    At the end of initState(), after all BLoC initial events are dispatched and
    before the closing brace of initState(), add:
    ```dart
    FlutterNativeSplash.remove();
    ```

    After editing, run:
    ```bash
    fvm flutter analyze lib/main.dart lib/presentation/app_initializer.dart
    fvm flutter test
    ```

    Then re-test cold launch on iOS simulator to confirm the white flash is gone.
  </action>
  <verify>
    <automated>cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm flutter analyze lib/main.dart lib/presentation/app_initializer.dart && fvm flutter test</automated>
  </verify>
  <acceptance_criteria>
    Either:
    (a) Task skipped because no white flash was observed — mark done with "not needed"
    OR
    (b) If flash was observed and fix was applied:
        - `grep 'FlutterNativeSplash.preserve' lib/main.dart` returns a result
        - `grep 'FlutterNativeSplash.remove' lib/presentation/app_initializer.dart` returns a result
        - `fvm flutter analyze lib/main.dart lib/presentation/app_initializer.dart` exits 0 with no errors
        - `fvm flutter test` exits 0
        - Cold launch re-test shows no white flash between native splash and first Flutter frame
  </acceptance_criteria>
  <done>White flash either was not present (task skipped) or was resolved by adding FlutterNativeSplash.preserve/remove; test suite is green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/main.dart → Flutter engine | WidgetsBinding.ensureInitialized() return value used to preserve splash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | FlutterNativeSplash.preserve (conditional) | accept | If preserve() is called but remove() is never called, splash stays indefinitely; mitigated by calling remove() in AppInitializer.initState() immediately after BLoC events — this runs within the first frame |
| T-06-05 | Spoofing | Splash colour | accept | Brand colour is cosmetic; no authentication or security claim is made by the splash screen |
</threat_model>

<verification>
After this plan completes:

**REL-01 verification:**
- iOS home screen: Weeklet icon is NOT the default Flutter blue/teal logo
- Android home screen: Weeklet icon uses #1447E6 adaptive background (if emulator tested)

**REL-02 verification:**
- iOS cold launch: Brand-blue splash visible before first Flutter frame
- Android 12+ cold launch: Brand-blue splash visible (not white) before first Flutter frame
- No white flash between native splash and app frame (or preserved splash removes cleanly)

**Regression check:**
- `fvm flutter test` exits 0 throughout
</verification>

<success_criteria>
- REL-01: Launcher icon is confirmed non-default on iOS simulator (and Android emulator if tested)
- REL-02: Native splash is brand-blue on cold launch for iOS; Android 12+ emulator shows blue not white
- White flash is either absent or resolved by FlutterNativeSplash.preserve/remove
- fvm flutter test is green
- Phase 6 is complete — app is ready for App Store and Google Play asset submission
</success_criteria>

<output>
After completion, create `.planning/phases/06-release-polish/06-02-SUMMARY.md` following the summary template at `.claude/get-shit-done/templates/summary.md`.
</output>
