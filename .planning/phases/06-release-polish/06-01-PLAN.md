---
phase: 06-release-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - assets/icon/icon.png
autonomous: false
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "assets/icon/icon.png exists and is a 1024x1024 opaque PNG"
    - "pubspec.yaml declares assets/icon/ under flutter: assets:"
    - "pubspec.yaml contains a complete flutter_launcher_icons: block with remove_alpha_ios: true and adaptive icon config"
    - "pubspec.yaml contains a complete flutter_native_splash: block with color, color_dark, image, and android_12: sub-block"
    - "fvm dart run flutter_launcher_icons exits 0 and writes icon files to iOS and Android platform directories"
    - "fvm dart run flutter_native_splash:create exits 0 and writes splash assets to iOS and Android platform directories"
    - "fvm flutter test passes with no new failures after all generation"
  artifacts:
    - path: "assets/icon/icon.png"
      provides: "Source icon for both generators"
    - path: "pubspec.yaml"
      provides: "Configuration for flutter_launcher_icons and flutter_native_splash"
      contains: "flutter_launcher_icons:"
    - path: "ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json"
      provides: "Generated iOS icon set metadata"
    - path: "android/app/src/main/res/mipmap-hdpi/ic_launcher.png"
      provides: "Generated Android launcher icon (hdpi)"
    - path: "android/app/src/main/res/drawable/launch_background.xml"
      provides: "Generated Android splash background"
  key_links:
    - from: "pubspec.yaml flutter_launcher_icons:"
      to: "ios/Runner/Assets.xcassets/AppIcon.appiconset/"
      via: "fvm dart run flutter_launcher_icons"
      pattern: "adaptive_icon_background.*1447E6"
    - from: "pubspec.yaml flutter_native_splash:"
      to: "android/app/src/main/res/drawable-v21/launch_background.xml"
      via: "fvm dart run flutter_native_splash:create"
      pattern: "color.*1447E6"
---

<objective>
Provide the source icon asset, configure both generator packages in pubspec.yaml,
and run both generators to produce professional iOS and Android launcher icons and
native splash screens.

Purpose: Replace the default Flutter blue icon and blank white splash with
Weeklet-branded assets before App Store and Google Play submission.
Output: Generated platform icon files in ios/Runner/Assets.xcassets/AppIcon.appiconset/
and android/app/src/main/res/mipmap-*/; generated splash files in
ios/Runner/Assets.xcassets/LaunchImage.imageset/ and
android/app/src/main/res/drawable/launch_background.xml.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-release-polish/06-RESEARCH.md

<interfaces>
<!-- Key facts the executor needs. Extracted from codebase. -->

From pubspec.yaml (current flutter: section):
```yaml
flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  # (commented-out scaffold placeholders — replace entire assets block)
```

From lib/core/theme/colors.dart (verified brand colours):
- Light primary / brand blue: #1447E6
- Dark background: #101828

Generator commands (FVM-pinned):
  fvm flutter pub get
  fvm dart run flutter_launcher_icons
  fvm dart run flutter_native_splash:create

Both packages already in pubspec.lock at exact versions:
  flutter_launcher_icons 0.14.4
  flutter_native_splash 2.4.7
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide source icon PNG</name>
  <files>assets/icon/icon.png</files>
  <action>
    Place a 1024×1024 opaque PNG at assets/icon/icon.png.

    Requirements for the file:
    - Exact dimensions: 1024×1024 pixels (square)
    - Format: PNG
    - Alpha channel: must be FULLY OPAQUE — no transparency (iOS App Store rejects icons with alpha)
    - No pre-applied rounded corners (iOS and Android apply their own masks at runtime)
    - Recommended design: Weeklet brand identity using #1447E6 (brand blue) as background

    Steps to create the assets/icon/ directory and place the file:
    1. Create directory: mkdir -p assets/icon in the project root
    2. Copy or export your 1024×1024 PNG to assets/icon/icon.png

    If you do not yet have a designed icon, a solid-color placeholder is acceptable
    for an initial build (e.g., a 1024×1024 blue square with a white W). You can
    replace it before final App Store submission.
  </action>
  <verify>
    <automated>test -f "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/assets/icon/icon.png" &amp;&amp; echo "ICON_EXISTS"</automated>
  </verify>
  <done>assets/icon/icon.png exists, is a square 1024×1024 opaque PNG, and file size is > 10 KB</done>
  <resume-signal>Type "icon ready" once assets/icon/icon.png is in place</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure pubspec.yaml (asset declaration + generator config blocks)</name>
  <read_first>
    - pubspec.yaml (read the full file — especially the flutter: section and the end of the file)
  </read_first>
  <files>pubspec.yaml</files>
  <action>
    Make two distinct edits to pubspec.yaml:

    EDIT 1 — Uncomment and replace the assets block inside the flutter: section.
    The current flutter: section ends with commented-out scaffold placeholders.
    Replace the entire commented assets/fonts block so the flutter: section becomes:

    ```yaml
    flutter:
      uses-material-design: true
      assets:
        - assets/icon/
    ```

    (Remove all the commented-out `# assets:` and `# fonts:` lines from the flutter: section.)

    EDIT 2 — Add two new top-level config sections at the very end of pubspec.yaml
    (after the closing of the flutter: section, as sibling top-level keys):

    ```yaml
    flutter_launcher_icons:
      android: 'launcher_icon'
      ios: true
      image_path: 'assets/icon/icon.png'
      min_sdk_android: 21
      remove_alpha_ios: true
      adaptive_icon_background: '#1447E6'
      adaptive_icon_foreground: 'assets/icon/icon.png'

    flutter_native_splash:
      color: '#1447E6'
      color_dark: '#101828'
      image: 'assets/icon/icon.png'
      android_12:
        color: '#1447E6'
        image: 'assets/icon/icon.png'
        icon_background_color: '#1447E6'
    ```

    IMPORTANT constraints:
    - `flutter_launcher_icons:` and `flutter_native_splash:` are top-level YAML keys —
      they must NOT be nested under `flutter:` or `dependencies:`
    - Preserve the existing dependencies, dev_dependencies, and environment sections exactly
    - Use single quotes for all string values (consistent with project lint: prefer_single_quotes)
    - Do not add any other keys or change any existing content
  </action>
  <verify>
    <automated>grep -n 'flutter_launcher_icons:' "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/pubspec.yaml" && grep -n 'flutter_native_splash:' "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/pubspec.yaml" && grep -n 'assets/icon/' "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/pubspec.yaml" && grep -n 'remove_alpha_ios: true' "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/pubspec.yaml" && grep -n 'android_12:' "/Users/vasilenovatchii/My Projects/Flutter/Weeklet/pubspec.yaml"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'flutter_launcher_icons:' pubspec.yaml` returns a top-level line (not indented)
    - `grep 'flutter_native_splash:' pubspec.yaml` returns a top-level line (not indented)
    - `grep 'assets/icon/' pubspec.yaml` appears under the `flutter:` section (indented with -)
    - `grep 'remove_alpha_ios: true' pubspec.yaml` returns a result
    - `grep 'android_12:' pubspec.yaml` returns a result
    - `grep 'adaptive_icon_background' pubspec.yaml` returns `'#1447E6'`
    - `grep 'color_dark' pubspec.yaml` returns `'#101828'`
    - `fvm flutter pub get` exits 0 (YAML is valid)
  </acceptance_criteria>
  <done>pubspec.yaml has a valid assets declaration in flutter: section and both generator config blocks as top-level sibling keys</done>
</task>

<task type="auto">
  <name>Task 3: Run flutter_launcher_icons generator</name>
  <read_first>
    - pubspec.yaml (confirm config block is present before running)
    - assets/icon/icon.png (confirm file exists — the generator will fail without it)
  </read_first>
  <files>
    ios/Runner/Assets.xcassets/AppIcon.appiconset/
    android/app/src/main/res/mipmap-mdpi/ic_launcher.png
    android/app/src/main/res/mipmap-hdpi/ic_launcher.png
    android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
    android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
    android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
  </files>
  <action>
    Run the icon generator from the project root:

    ```bash
    cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet"
    fvm flutter pub get
    fvm dart run flutter_launcher_icons
    ```

    The generator reads `flutter_launcher_icons:` from pubspec.yaml and writes:
    - iOS: all icon sizes into ios/Runner/Assets.xcassets/AppIcon.appiconset/
    - Android: ic_launcher.png into all mipmap-* density directories
    - Android adaptive: foreground/background layers and XML descriptors (API 26+)

    If the command fails with "Image not found" → the icon.png path is wrong or missing.
    If the command fails with "No configuration found" → check that flutter_launcher_icons: is
    a top-level key in pubspec.yaml (not nested).

    After the generator succeeds, run the regression suite:
    ```bash
    fvm flutter test
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm dart run flutter_launcher_icons && echo "ICONS_OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `fvm dart run flutter_launcher_icons` exits 0 and prints "ICONS_OK" (or generator success message)
    - `ls ios/Runner/Assets.xcassets/AppIcon.appiconset/` lists multiple PNG files (not just the old 3-file default)
    - `ls android/app/src/main/res/mipmap-hdpi/` includes `ic_launcher.png` with a file size > 1 KB (replaced from 72-byte default)
    - `fvm flutter test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>All iOS and Android launcher icon assets are generated from the brand source image and the test suite remains green</done>
</task>

<task type="auto">
  <name>Task 4: Run flutter_native_splash generator</name>
  <read_first>
    - pubspec.yaml (confirm flutter_native_splash: config block is present)
  </read_first>
  <files>
    ios/Runner/Assets.xcassets/LaunchImage.imageset/
    ios/Runner/Base.lproj/LaunchScreen.storyboard
    android/app/src/main/res/drawable/launch_background.xml
    android/app/src/main/res/drawable-v21/launch_background.xml
    android/app/src/main/res/values/launch_background.xml
  </files>
  <action>
    Run the splash screen generator from the project root:

    ```bash
    cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet"
    fvm dart run flutter_native_splash:create
    ```

    The generator reads `flutter_native_splash:` from pubspec.yaml and writes:
    - iOS: updates LaunchScreen.storyboard background colour to #1447E6, writes
      brand-coloured launch images to LaunchImage.imageset/
    - Android (pre-12): updates drawable/launch_background.xml with brand colour
    - Android 12+: generates a new-style splash from the android_12: config block,
      writing to values-v31/ or drawable-v31/ as required

    If the command fails with "No configuration found" → check that flutter_native_splash:
    is a top-level key (not nested under flutter:).
    If it fails with image not found → verify assets/icon/icon.png path.

    After the generator succeeds, run the regression suite:
    ```bash
    fvm flutter test
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/vasilenovatchii/My Projects/Flutter/Weeklet" && fvm dart run flutter_native_splash:create && echo "SPLASH_OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `fvm dart run flutter_native_splash:create` exits 0 and prints "SPLASH_OK" (or generator success message)
    - `grep -r '1447E6' android/app/src/main/res/drawable/` returns at least one match (brand colour written to XML)
    - `grep -r '1447E6' android/app/src/main/res/` returns matches in at least 2 files (includes android_12 style)
    - `ls ios/Runner/Assets.xcassets/LaunchImage.imageset/` lists PNG files larger than 68 bytes (replaced from blank defaults)
    - `fvm flutter test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>iOS and Android splash screen assets are generated with brand-blue background (#1447E6) including the android_12: block for API 31+ devices, and the test suite remains green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → generators | Generator reads config from pubspec.yaml written by Claude |
| Generator output → platform build | Generated PNG/XML files are consumed by Xcode/Gradle at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | assets/icon/icon.png | accept | Source PNG is a static design asset with no executable content; risk is cosmetic only |
| T-06-02 | Tampering | pubspec.yaml config blocks | accept | YAML is committed to git; any modification is traceable via version control |
| T-06-03 | Denial of Service | Generator commands | accept | Generators run at build time only, not at runtime; no attack surface exposed to users |
</threat_model>

<verification>
After all four tasks complete:

1. `grep 'flutter_launcher_icons:' pubspec.yaml` → top-level key present
2. `grep 'flutter_native_splash:' pubspec.yaml` → top-level key present
3. `grep 'assets/icon/' pubspec.yaml` → declared under flutter: assets:
4. `grep 'remove_alpha_ios: true' pubspec.yaml` → iOS alpha removal configured
5. `grep 'android_12:' pubspec.yaml` → Android 12+ block present
6. `ls ios/Runner/Assets.xcassets/AppIcon.appiconset/` → multiple generated PNG files
7. `grep -r '1447E6' android/app/src/main/res/drawable/` → brand colour in generated splash XML
8. `fvm flutter test` → exits 0, all existing tests pass
</verification>

<success_criteria>
- assets/icon/icon.png exists and is a 1024×1024 opaque PNG
- pubspec.yaml contains both generator config blocks as top-level keys
- flutter_launcher_icons generator exits 0 and overwrites iOS AppIcon.appiconset and Android mipmap directories
- flutter_native_splash generator exits 0 and overwrites iOS LaunchImage.imageset, LaunchScreen.storyboard, and Android launch_background.xml (including android_12 style)
- fvm flutter test exits 0 after all generation — no regressions in existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-release-polish/06-01-SUMMARY.md` following the summary template at `.claude/get-shit-done/templates/summary.md`.
</output>
