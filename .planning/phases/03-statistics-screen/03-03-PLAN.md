---
phase: 03-statistics-screen
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: []
autonomous: false
requirements: [STAT-01, STAT-02, STAT-03, STAT-04]

must_haves:
  truths:
    - "Statistics screen shows no WIP artifacts — all text is English, currency uses correct symbol"
    - "Monthly totals on stats screen match the sum of entries visible on the expense/income list screens"
    - "Annual evolution tab shows bars for up to 12 months of the selected year"
    - "Switching between 'All Months' and a specific month updates card labels to 'Annual' / 'Monthly'"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm STAT-01 through STAT-04 are visually and functionally
correct in the running app.

Purpose: The automated tests cover STAT-03 and STAT-04 at the unit level. STAT-01 (no WIP
artifacts) and STAT-02 (totals accuracy) require human eyes and live Hive data to verify.

Output: User sign-off that all four STAT requirements are met.
</objective>

<execution_context>
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilenovatchii/My Projects/Flutter/Weeklet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-statistics-screen/03-CONTEXT.md
@.planning/phases/03-statistics-screen/03-01-SUMMARY.md
@.planning/phases/03-statistics-screen/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify STAT-01 through STAT-04 in running app</name>
  <files>none (verification only)</files>
  <action>Run the app with `fvm flutter run` and follow the verification steps below.</action>
  <what-built>
    Plan 01: 12-month evolution stats, hoisted Hive reads, StatsFailure rename, use case param cleanup.
    Plan 02: Dynamic card labels (Annual/Monthly), 'vs last year' badge, currency fixes, Romanian string fixes, shouldRebuild=false.
  </what-built>
  <how-to-verify>
    Run the app first:
    ```
    fvm flutter run
    ```

    **STAT-01 — No WIP artifacts:**
    1. Navigate to the Statistics screen.
    2. Confirm AppBar shows "Statistics" (English).
    3. Select "All Months" in the month dropdown — confirm the top cards show
       "Annual Income", "Annual Expenses", "Annual Balance".
    4. Select any specific month — confirm the top cards switch to
       "Monthly Income", "Monthly Expenses", "Monthly Balance".
    5. In the Annual Evolution tab, check the monthly list items:
       - Confirm "Income" and "Expenses" labels (not "Venit"/"Cheltuieli")
       - Confirm balance text shows "Balance: +X MDL" format (not "Sold: +X lei")
       - Confirm currency amounts use MDL (not RON)
    6. In the Monthly tab with data, check category details:
       - Confirm amounts show MDL format (not "X.XX RON")
    7. Confirm percentage badges show "vs last month" for a specific month and
       "vs last year" when "All Months" is selected.

    **STAT-02 — Correct totals:**
    1. Add 2–3 expenses on the Expenses screen for the current month with known amounts.
    2. Navigate to Statistics → select current month.
    3. Confirm the "Monthly Expenses" total matches the sum of entries on the expense screen.
    4. Add an income entry on the Income screen.
    5. Confirm the "Monthly Income" total on Statistics updates after the income is added.

    **STAT-03 — Annual overview covers 12 months:**
    1. Navigate to Statistics → Annual Evolution tab.
    2. Select "All Months" in the month filter.
    3. Confirm the bar chart and monthly list show entries only for months with data
       (no empty-month artifacts). If you have data spread across more than 6 months in the
       selected year, verify all months appear (old 6-month cap is gone).

    **STAT-04 — No redundant reads (visual check):**
    4. Open the Statistics screen and switch between month filters multiple times.
    5. The screen should respond without noticeable lag (Hive reads reduced from 12x to 2x
       per evolution stats call).
  </how-to-verify>
  <verify>Human approval via resume-signal below.</verify>
  <done>User types "approved" confirming all four STAT requirements are met in the running app.</done>
  <resume-signal>
    Type "approved" if all four STAT requirements are met.
    Or describe any issues observed so they can be fixed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → App | Read-only statistics screen; no input, no trust concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Statistics display | accept | Local Hive data only; user is verifying their own data |
</threat_model>

<verification>
Human sign-off on:
- [ ] STAT-01: No Romanian strings, no hardcoded RON/lei, dynamic labels work
- [ ] STAT-02: Monthly totals match list screens
- [ ] STAT-03: Annual evolution shows correct year scope (not capped at 6 months)
- [ ] STAT-04: Performance acceptable; no redundant read slowness observed
</verification>

<success_criteria>
User types "approved" after verifying all four STAT requirements in the running app.
All four requirements are marked complete in REQUIREMENTS.md.
</success_criteria>

<output>
After user approval, create `.planning/phases/03-statistics-screen/03-03-SUMMARY.md`
</output>
