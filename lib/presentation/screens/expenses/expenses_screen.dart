import 'package:flutter/material.dart';

import 'widgets/expenses_view.dart';

class ExpensesScreen extends StatelessWidget {
  const ExpensesScreen({super.key});

  @override
  Widget build(BuildContext context) => const ExpensesView();
}
