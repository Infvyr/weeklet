import 'package:flutter/widgets.dart';

class ExpenseFormDateView extends StatelessWidget {
  const ExpenseFormDateView({super.key});

  @override
  Widget build(BuildContext context) => const Text('Expense Form Date View');
}
